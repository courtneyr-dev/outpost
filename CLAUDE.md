# Outpost

## Project

- **Slug:** outpost
- **Text Domain:** outpost
- **Prefix:** `outpost_` (functions, options); `Outpost_` (classes); `OUTPOST_` (constants)
- **Min WP:** 6.5 | **Tested up to:** 6.9 | **Min PHP:** 8.2
- **Repo:** https://github.com/courtneyr-dev/outpost
- **Tagline:** *Post from your outpost. Reach your people everywhere.*

## What It Does

Mobile-first PWA composer at `/post` (configurable). Five modes: Note, Reply/Like/Repost/Bookmark/RSVP/Follow, Listen/Watch/Read/Checkin/Play, Photo, Article. Optimised for IndieWeb post shapes and life-tracking entries. POSSE-first: every configured syndication destination is on by default per post. iOS-primary, Android-compatible. No Jetpack, no app store, no third-party auth.

Uses Micropub as the API and IndieAuth for browser-side auth. PWA frontend uses Vite + TypeScript + Preact, output to `/build/pwa/`.

## Hard Contract — plugin owns layout, theme owns paint

If anything else in this file contradicts this rule, the rule wins.

### Plugin always owns

- The PWA shell, routes, service worker, manifest, offline behavior.
- Composer interaction: tab structure, syndication chips, voice-input slot, file upload UI.
- The Micropub client implementation.
- IndieAuth flow and encrypted IndexedDB token storage.
- Structural CSS: layout grids, iOS safe-area insets, touch-target sizes, focus states.
- Capability detection for companion plugins.
- CSS custom property *names* and structural defaults.

### Theme always owns

- Every color value.
- Every font choice.
- Spacing scale, border radius, shadow, surface treatment.
- Button fills, accent colors, link colors.
- Whether the PWA inherits the site's block theme typography or sets its own.

### The handshake

Plugin structural CSS only references colors and fonts through `var(--outpost-*, neutral-fallback)`:

```css
.outpost-composer {
	background: var(--outpost-surface-bg, transparent);
	color: var(--outpost-surface-fg, inherit);
}
.outpost-syndication-chip[aria-pressed="true"] {
	background: var(--outpost-chip-active-bg, currentColor);
	color: var(--outpost-chip-active-fg, inherit);
}
```

If a theme sets the variable, the plugin uses it. Otherwise the composer inherits the theme's normal styling. **No part of Outpost ever forces a color the theme didn't ask for.**

One forced default: `padding-bottom: env(safe-area-inset-bottom)` on the iOS bottom toolbar. Structural, not paint.

## Companion Strategy

Detection runs at runtime (not install-time) so the UI updates as users activate plugins. Adapter classes live at `includes/companions/`. Composer code never imports companion-specific code directly.

The dependency chain is **upstream-first**: `[indieauth/indieauth.php, micropub/micropub.php]`. Notice rendering and REST 503 reasons short-circuit on the first unsatisfied entry — installing a downstream dependency before the upstream one is met produces a broken state (Micropub plugin's own preflight fails without IndieAuth).

| Companion | Outpost behavior |
|-----------|------------------|
| `indieauth/indieauth.php` (Pfefferle, Shanske) | **Required (plugin install).** The Micropub plugin hard-requires IndieAuth at its own preflight, so Outpost surfaces IndieAuth status as the most-upstream notice. App passwords are *not* an alternative to installing the IndieAuth plugin — they remain a runtime fallback only when the IndieAuth endpoint is unreachable for an already-installed IndieAuth plugin. Detected by `outpost_indieauth_status()`. |
| `micropub/micropub.php` (David Shanske) | **Required (plugin install).** Hybrid gate: plugin loads, admin notice when any chain entry is absent or inactive, PWA route renders friendly install page, REST routes return 503 with the chain entry name as the reason. Detected by `outpost_micropub_status()`. |
| Post Kinds for IndieWeb | Surface Listen/Watch/Read/Checkin/Play tabs. Surface Follow sub-mode. |
| Post Formats for Block Themes | Format selector in More pull-out. Auto-detect from content. |
| Link Extension for XFN | Relationship picker on reply targets. |
| Syndication Links | Read configured destinations into chip UI. |
| Yoast SEO | Focus keyphrase + meta description fields in More pull-out. |
| ActivityPub / Bridgy | Surface as syndication destinations automatically. |

## Bridgy Auto-Suggest

When the source URL host of a Reply/Like/Repost/Bookmark matches a Bridgy-supported network, the corresponding Bridgy publish URL is added as a syndication chip and enabled by default. Mapping lives at `includes/class-bridgy-detector.php` and is filterable via `outpost_bridgy_host_map`. Per-post and global opt-out toggles.

## Build Commands

```bash
composer install && npm install
npm run dev             # Vite dev server for PWA frontend
npm run build           # Production PWA build to /build/pwa/
composer test           # PHPUnit
composer lint           # PHPCS (WordPress-Extra)
composer analyze        # PHPStan
npm run lint            # ESLint
npm run test:e2e        # Playwright
```

## File Layout

```
outpost.php                 Bootstrap (header, requirements, Micropub gate)
includes/                   PHP classes
includes/companions/        Adapter classes for each companion plugin
admin/                      Settings page, bookmarklet generator, onboarding
pwa/src/                    PWA frontend source (TypeScript + Preact + Vite)
pwa/src/modes/              One file per composer mode
pwa/src/components/         Shared composer UI components
pwa/src/lib/                Micropub client, IndieAuth, token store, offline queue
pwa/src/styles/tokens.css   --outpost-* token defaults
styles/outpost-tokens.css   Server-rendered token defaults
build/                      Compiled PWA assets (gitignored)
assets/icons/               PWA icons at all required sizes
tests/unit/                 WP_Mock unit tests
tests/integration/          WP_UnitTestCase integration tests
tests/e2e/                  Playwright e2e
docs/                       User and integrator docs
docs/security/              CSP, threat-model docs
languages/                  POT and MO files
bin/                        Helper scripts
```

## Standards

- WordPress PHP + JS Coding Standards (tabs, Yoda conditions).
- Security Trinity: sanitize input → validate data → escape output.
- All strings wrapped in `__()` / `_e()` with text domain `outpost`.
- All blocks (when added) use `apiVersion: 3`.
- Escape at render: `esc_html()`, `esc_attr()`, `esc_url()`, `wp_kses_post()`.
- DB queries via `$wpdb->prepare()` only.
- Nonces + `current_user_can()` on every admin and REST action.
- TypeScript strict mode, no `any` (use `unknown` + type guards).
- Token in encrypted IndexedDB, never localStorage or cookies.
- Service worker scope is `/post/` only, never the whole site.

## WordPress.org Compliance

Outpost is GPLv2-or-later, free, fully functional. Per [WordPress.org Plugin Guideline §5](https://developer.wordpress.org/plugins/wordpress-org/detailed-plugin-guidelines/#5-trialware-is-not-permitted), every feature in the WordPress.org version must work without payment. There are no:

- License keys
- Trial periods or time limits
- Usage quotas
- Features gated by payment
- Intrusive nag screens

**Adapter behavior corollary (Phase F):** when Outpost integrates with paid companion plugins (Yoast SEO Premium, paid ActivityPub variants), the *integration* may detect the paid plugin's presence and surface its features (focus keyphrase, custom federation routing). But Outpost's *own* functionality (note posting, reply, photo, etc.) MUST work whether or not the user has the paid companion. Detection enables; absence does not disable Outpost.

**Reject contributions that propose:**
- A "Pro" tier of Outpost itself
- Feature flags gated by license keys
- Time-limited trials
- Per-post or per-action quotas
- Adapter logic that disables Outpost features when a free companion is present but a paid one isn't

Informational comparison tables and feature-detection patterns are fine. The Hard Contract above (plugin owns layout, theme owns paint) takes precedence — Outpost's UI must not become an upsell surface for any third-party plugin or service either.

## Security Hot Spots

- **Server-side mf2 preview** (`/wp-json/outpost/v1/preview`) is the SSRF surface. Use `wp_safe_remote_get`, cap response size at 5MB, validate content-type, strip scripts before returning. See Section 8.4 of the original prompt and `docs/security/csp.md`.
- **Required alt text on photos.** Structural, not configurable. Decorative toggle submits `alt=""`.
- **EXIF stripping on upload.** Privacy: prevents location leak from camera roll.
- **Bookmarklet `url` parameter validation** at the composer endpoint: reject non-http(s), reject internal IPs, length-cap at 2048 chars.

## Prior Art

Outpost evolves from prior IndieWeb work for WordPress. Credit these in `readme.txt`, `README.md`, and the bookmarklet settings section.

- **IndieWeb Press This** (Pfefferle, Shanske, Barrett) — bookmarklet pattern for Reply/Like/Repost/RSVP/Follow from any page. Outpost extends and modernizes.
- **Micropub plugin** (David Shanske) — required dependency, our server endpoint.
- **IndieAuth plugin** (Pfefferle, Shanske) — auth.
- **IndieBlocks** (Jan Boddez) — companion for theme blocks.
- **Bridgy** (Ryan Barrett) — round-trip syndication service.

## Forbidden Vocabulary

In commits, code comments, copy, admin labels, and docs: avoid *delve, leverage, synergy, robust, seamless, ecosystem (non-tech sense), stakeholder, bandwidth (non-tech sense), pivot, agentic AI, AI agents*. Use plain language: "uses" not "leverages," "thorough" not "robust," "smooth" not "seamless," "people involved" not "stakeholders."

## Session A1 — Locked Decisions (shipped)

Closed 2026-05-01. The constraints below are now production code; the rationale stays here so future sessions don't re-litigate.

1. **`Outpost_Companion_Detector` is `final`, static-only.** No per-instance state — every caller (admin notices, PWA install prompt, REST 503 reasons) wants a single function-call answer against WordPress globals. `outpost.php`'s procedural functions (`outpost_companion_plugin_status`, `outpost_indieauth_status`, `outpost_micropub_status`) are thin shims that delegate to the static methods. Old call sites keep working; new code calls the class directly.
2. **`Outpost_Companion_Detector::dependency_chain()` is the single source of truth** for the IndieAuth → Micropub ordering. `first_unsatisfied()` short-circuits on the first miss. `outpost_is_ready()` calls `first_unsatisfied()` instead of hard-coding the order, so future chain extensions propagate automatically.
3. **IndieAuth is hard-required at the plugin-install level**, not at the runtime auth level. Discovered during Session A0 staging test 2026-05-01: the WordPress.org Micropub plugin self-disables without IndieAuth installed and active. The original prompt's "fall back to WordPress application passwords" path is moot. Application passwords remain a fallback only for the Micropub bearer token at runtime when an already-installed IndieAuth endpoint is unreachable, not as an alternative to installing the plugin. **Session B0 plan to be updated when reached** — the IndieAuth flow is required, not optional.
4. **`Outpost_Companion_Base` is abstract.** Three abstract methods every adapter must implement: `file()` (which `OUTPOST_*_PLUGIN_FILE` it wraps), `label()` (brand name), `capabilities()` (string[] of capability slugs). Two `final` concrete methods every adapter inherits: `status()` and `is_active()` — both delegate to the detector. Phase F adapters declare identity, never fork detection logic.
5. **`capabilities(): array` returns a flat slug list (Option A locked).** Adapters return things like `['post-kinds.listen', 'post-kinds.watch', 'syndication.chips']`. The composer in later sessions does `in_array('post-kinds.listen', $caps, true)`. Promotion to Option B (per-capability config map) only if a Phase F adapter surfaces a real need — premature structure now would just be guessing at requirements.
6. **Notice presentation map (`OUTPOST_INDIEAUTH_PLUGIN_FILE => ['IndieAuth', 'indieauth']`) is co-located with the consumer**, not baked into the detector. The detector is concerned with state; presentation belongs with whoever renders it.
7. **`_doing_it_wrong()` guards the silent-drop branch** in `outpost_render_admin_notices()`. If `dependency_chain()` ever extends without a matching presentation entry, the gap surfaces in Query Monitor's `doing_it_wrong` panel instead of disappearing as a missing notice. Pairs with `WP_DEBUG=false` because the `doing_it_wrong_run` action fires unconditionally.
8. **PHPUnit is pinned to `^9.6` because of `10up/wp_mock`'s Mockery compat window.** PHPUnit 9.6 has support through 2026; promoting to 10.x will wait until WP_Mock supports it. Test namespaces split into `Outpost\Tests\Unit\` and `Outpost\Tests\Integration\` so each suite has a clean PSR-4 root.
9. **Yoast file path is `wordpress-seo/wp-seo.php`**, not the slug-derived `wordpress-seo/wordpress-seo.php`. The slug-vs-file mismatch is a known gotcha for Yoast — captured as test data in `provide_companion_files()` so a regression flips the matrix.

## Session A2 — Locked Decisions (shipped)

Closed 2026-05-01 (same day as A1; A1 + A2 deployed together to staging because A1 was scaffolding A2 consumed). The constraints below are now production code; the rationale stays here so future sessions don't re-litigate.

1. **`outpost_dependency_presentation($plugin_file): ?array` is a procedural function in `outpost.php`, not a static method on the detector.** The detector is concerned with state (active/inactive/absent); presentation belongs with whoever renders it. Two consumers share the helper: `outpost_render_admin_notices()` and `Outpost_PWA_Shell::render_install_prompt()`. Returns `array{label:string,slug:string}` for known files, `null` otherwise.
2. **Filter `outpost_dependency_presentation` extends or overrides the map.** Future chain extensions and third-party integrations register through this filter without editing core. Filter signature: `apply_filters('outpost_dependency_presentation', array<string, array{label:string,slug:string}>)`. Locked associative shape (not positional `[label, slug]`) so filter callers can read by name.
3. **Five `/post/*` rewrite rules in upstream-first order.** Specific routes register before the catch-all so `/post/manifest.json` and `/post/sw` don't get hijacked by the composer regex:
   - `^post/manifest\.json$` → `manifest`
   - `^post/sw/?$` → `sw` (extension-less on purpose; see #14)
   - `^post/share-target/?$` → `share-target` (Phase E body)
   - `^post/auth/callback/?$` → `auth-callback` (Phase B body)
   - `^post/?$` → `composer`
   Every rule registers with the `top` flag so the order survives WP's internal rewrite-rule sort. The single source of truth is `Outpost_Route_Handler::rules()` — tests assert the whole table at once.
4. **`Outpost_Route_Handler::QUERY_VAR === 'outpost_route'`.** The query var carries the matched target (`composer`, `manifest`, etc.) into `template_redirect` where `dispatch()` hands off to the right `Outpost_PWA_Shell` method. Unknown values silently no-op so WP's normal 404 still applies.
5. **`Outpost_PWA_Shell` has two main rendering branches and two artefact endpoints.** `render()` branches on `outpost_is_ready()`: `render_shell()` emits the composer envelope HTML; `render_install_prompt()` consumes `first_unsatisfied()` + `outpost_dependency_presentation()` to produce the install/activate UI. `render_manifest()` and `render_service_worker()` emit JSON and JS with correct content-types. The composer body is intentionally empty in A2 — Phase C lands the modes.
6. **Service-worker scope is `/post/`** in both the registration script (`navigator.serviceWorker.register('/post/sw', { scope: '/post/' })`) and the manifest scope/start_url. The SW never tries to control the parent WordPress site (Standards §128 in this file).
7. **Activation registers rules eagerly + flushes.** `outpost_activate()` calls `Outpost_Route_Handler::register_rewrite_rules()` then `flush_rewrite_rules()` so the rules land in the rewrite cache without requiring the user to visit Settings → Permalinks. Deactivation only flushes — the rules drop naturally because the `init` hook stops firing.
8. **WP_Mock 1.x has a static-state leak via `Filter::$filtersWithAnyArgs`.** `withAnyArgs()` writes to a class-level static that `flush()` doesn't clear; the next test's `apply_filters` returns a random integer instead of the input. Workaround: `setUp()` resets the static with `ReflectionClass::getProperty()->setValue(null, [])`. Pattern travels to any future test file that calls `WP_Mock::onFilter()->withAnyArgs()`.
9. **Integration test for the rewrite flow is stubbed via `markTestSkipped`.** `tests/integration/RouteHandlerIntegrationTest.php` documents the assertions the wp-env-backed test will make (Content-Type per route, query-var dispatch, register_activation_hook side effects). Lands in a later session when wp-env is wired up — A2 can't run it because real WP_Rewrite needs a real WordPress core.
10. **REST 503 reasons stay deferred to B2.** `outpost_is_ready()` already drives them today via `first_unsatisfied()`; B2 just adds the `/wp-json/outpost/v1/*` route handler that returns the JSON 503 body. No work in A2.
### Decisions #11–#16: deployment + hosting gotchas (with scope tags)

Each is tagged with where it applies. The TypeScript port (indieweb-astro) and any other WordPress project pulling decisions from this file should filter on scope.

11. **[universal — applies to every WP plugin deployed via rsync]** Version-pinned rewrite-rule flush on `init`. Discovered during A2 staging verify on 2026-05-01: the activation hook only fires when an admin clicks "Activate", not when a deploy/update replaces the plugin in place. Without a flush guard, the cached `rewrite_rules` option holds the previous version's rule table and `/post/*` falls through to WP's canonical-redirect logic (which sent us to the most-similar post slug). `outpost_maybe_flush_rewrite_rules()` compares `OUTPOST_VERSION` against the stashed `outpost_rewrite_version` option and flushes once on mismatch. Hooked at `init` priority 11 so it lands right after `Outpost_Route_Handler::register_rewrite_rules` at priority 10.
12. **[universal — WordPress lifecycle, not hosting-specific]** Render methods call `Outpost_PWA_Shell::halt()` after writing the response. Without `exit`, WP continues past `template_redirect` and renders the theme template, concatenating it onto our shell/manifest/sw output. The unit-test bootstrap defines `OUTPOST_TESTING_PWA_SHELL` so `halt()` is a no-op in tests and `ob_start` can still capture render output for assertions.
13. **[universal — WordPress hook ordering, not hosting-specific]** `template_redirect` dispatch hooks at priority 1. WordPress's own `redirect_canonical` runs at priority 10 and was registered earlier in `default-filters.php`, so a same-priority handler loses the race. Priority 1 guarantees the route handler always sees the request before WP can 302 it into a trailing-slash variant.
14. **[managed-WP — applies to GoDaddy, WP Engine, Kinsta, and most nginx-fronted hosts]** Service worker URL has no `.js` extension — locked at `/post/sw`, registered via `navigator.serviceWorker.register('/post/sw', { scope: '/post/' })`. Most managed-WP hosts configure nginx to short-circuit `.js` requests with a static-file lookup before WordPress runs; with the extension our SW returns nginx 404 even when the rewrite rule is correct. Stripping the extension keeps the request in WP's hands. Discovered during A2 staging verify when `/post/sw.js` returned 404 nginx in 107ms while `/post/manifest.json` rendered correctly. The browser doesn't care about the script URL's extension as long as the response is JavaScript. Same caution applies to any future `.css`, `.png`, `.ico` paths that need to hit PHP — use extension-less URLs.
15. **[managed-WP — applies to any host with an edge cache that caches redirect responses]** Cache-buster query string needed for staging verify. GoDaddy's edge cache promotes 302/301 responses to cached entries (header `x-gateway-cache-key`); WP Engine and Kinsta have similar behavior with their own gateways. After deploying a fix, hitting the same URL serves the stale cached redirect. Append `?_cb=<timestamp>` (or any unique query parameter) when verifying staging changes — the new query string maps to a new cache key and forces a real PHP request. Confirmed by `x-gateway-skip-cache: 1` on the response.
16. **[universal — companion to #11]** Bump `OUTPOST_VERSION` in the same commit as any rewrite-rule table change. Decision #11's flush guard fires only on mismatch between `OUTPOST_VERSION` and the stashed `outpost_rewrite_version` option. If you change the rule table without bumping the version, deployed sites silently keep the old rule cache and the new routes don't activate. Caught my own footgun on 2026-05-01: shipped the `^post/sw/?$` rule without bumping, staging kept matching `^post/sw\.js$` until `0.1.0 → 0.1.1`. Both the plugin header `Version` and `OUTPOST_VERSION` constant must move together; they live two lines apart in `outpost.php`. Same rule applies to any future change in `Outpost_Route_Handler::rules()`, query-var names, or activation-hook side effects.

## Session A3 — Design Constraints (deferred — Phase B took priority)

A2's PWA shell renders an empty composer envelope with no styling tokens or static assets. A3 lands the structural CSS, the `--outpost-*` token defaults, and the icon set. Honor these when starting:

1. **`styles/outpost-tokens.css` is server-rendered, not bundled.** Themes need to inspect the cascade and override; a Vite-bundled-and-hashed token file makes that fragile. Keep the token file at a stable URL.
2. **The forced `padding-bottom: env(safe-area-inset-bottom)` on the iOS bottom toolbar is the only paint default Outpost ships.** Hard Contract above. Anything else is theme territory.
3. **Service worker fetch handler stays out of A3.** A2 ships a no-op SW so the registration script succeeds; the real fetch/cache strategy lands in Phase D after the composer modes exist (otherwise we cache a shell that's about to be replaced).

## Session B0a — Locked Decisions (shipped)

Closed 2026-05-01. The constraints below are now production code; the rationale stays here so future sessions don't re-litigate. B0a is the build pipeline + IndieAuth client + token store, no UI. B0b lands the login screen and the staging smoke test.

1. **Vite + Preact + manifest mode for the build pipeline.** Source at `pwa/src/`, output to `build/pwa/`, entry `pwa/src/index.tsx`. `build.manifest: true` produces `build/pwa/.vite/manifest.json` mapping source paths to hashed output filenames. B0b reads the manifest from PHP to enqueue the right `<script type="module">` — never hard-code a hash.
2. **`@preact/preset-vite`, not `@vitejs/plugin-preact`.** The latter is a hallucinated package name from A0's `package.json`; the canonical Preact-Vite integration is `@preact/preset-vite`. Fixed in B0a.
3. **`tsconfig.json` `include` is `pwa/src/**` only.** Vite + Vitest config files are excluded. Vitest brings its own pinned Vite version whose `Plugin` type diverges from the top-level Vite under `exactOptionalPropertyTypes: true` — writing casts to satisfy both isn't worth it. Config files run at build time; they don't need to pass app-grade typecheck.
4. **TypeScript strict ceiling: `strict + noUncheckedIndexedAccess + exactOptionalPropertyTypes + noImplicitOverride + noImplicitReturns + noFallthroughCasesInSwitch`.** Static safety where tests can't reach (public-API typing, exhaustive switch coverage, no-undefined-leaks-into-defined-shapes).
5. **happy-dom for the vitest environment, `fake-indexeddb/auto` for IDB-using tests.** happy-dom provides DOMParser + window + fetch shape; it does not provide IndexedDB. Token-store tests `import 'fake-indexeddb/auto'` and create a fresh `IDBFactory` per test so state never leaks between cases.
6. **Native `crypto.subtle` from Node 19+, used via `globalThis.crypto`.** No webcrypto polyfill needed for vitest. Tests run AES-GCM and SHA-256 against real Web Crypto. The IndieAuth + token-store libraries reach the same APIs in the browser via the same `globalThis.crypto` reference.
7. **Injectable environment pattern.** `IndieAuthEnvironment { fetch, crypto, random }` and `TokenStoreEnvironment { indexedDB, crypto }` flow into every public function as an optional second argument that defaults to globals. Tests pass deterministic stubs (fixed `random()`, mocked `fetch`, `fake-indexeddb` factories) without touching globals. Future `MicropubClient` and `OfflineQueue` follow the same shape.
8. **`detect_route(pathname)` reads `location.pathname`, not the shell's `data-outpost-route` attribute.** The PHP shell hard-codes `data-outpost-route="composer"` because the route handler dispatches `composer`, `share-target`, and `auth-callback` to the same `render_shell()`. The JS bundle is the only place that knows which of those three URLs the user actually landed on.
9. **Token store threat model — defeats DevTools inspection, does not defeat same-origin XSS.** AES-GCM 256-bit with non-extractable `CryptoKey` persisted in IndexedDB via structured clone. `crypto.subtle.exportKey()` refuses to dump raw bytes for non-extractable keys, raising the bar past simple IDB inspection. JS in the same origin can still call `read_token()` and get plaintext — that's the same threat any localStorage scheme has and why the CSP work in Phase G is the real defense. Documented honestly in the file's docblock so the next reader doesn't overestimate the protection.
10. **`nocache_headers()` on every HTML response from `Outpost_PWA_Shell::send_html_header()`.** Managed-WP page caches (Varnish on GoDaddy, nginx FastCGI on others) cache anonymous responses by default. Without `nocache_headers()` an install-prompt or auth-callback page rendered for one user could be served to another. Manifest and SW responses keep their existing cache semantics — `nocache_headers()` is HTML-only.
11. **`build/pwa/` gitignore tension flagged for B0b.** The `gd-wordpress-deployer` rsync reflects the working tree, so a production-shaped staging deploy needs committed build output. B0a's `.gitignore` keeps `build/` ignored; B0b will either un-ignore `build/pwa/` (mirrors the courtneyr-child theme pattern of committing built CSS) or wire a CI step that builds + force-adds to a deploy branch. Decision deferred to B0b because B0a ships no observable change to staging.

## Session B0b — Locked Decisions (shipped)

Closed 2026-05-01. The constraints below are now production code; the rationale stays here so future sessions don't re-litigate. B0b lands the login screen UI, the PHP manifest reader, the auth-callback handler, and the build-artefact tracking story.

1. **`Outpost_PWA_Assets` is `final`, static-only, with per-request caching.** Reads `build/pwa/.vite/manifest.json` once per request via `manifest()`. `entry_url()` resolves the hashed JS file URL; `entry_css_urls()` resolves the entry's CSS chunks (`css` array on the manifest record). All three fail gracefully — null/empty when the manifest is missing or invalid — so dev environments without a build still render the install-prompt path cleanly.
2. **`override_paths_for_tests()` is the only test seam on Outpost_PWA_Assets.** The class reads `OUTPOST_PLUGIN_DIR` and `OUTPOST_PLUGIN_URL` directly in production; tests substitute a temp directory + fake URL prefix via the override. Pattern reusable for future filesystem-touching classes.
3. **`build/pwa/` is committed to git** via `build/*` (ignore contents) + `!build/pwa/` (re-include) in `.gitignore`. The `gd-wordpress-deployer` rsync reflects the working tree, so the production build needs to ride along. `npm run build` is part of the deploy ritual, documented in `docs/STAGING-DEPLOY.md`. Symptom of forgetting: empty `<main id="outpost-root">` on staging, no console activity from the bundle.
4. **`/.vite/` anchored at root.** Vite's dev cache lives at `<project>/.vite/`; the production manifest lives at `build/pwa/.vite/manifest.json`. Anchoring with a leading slash keeps the dev cache ignored while letting the production manifest through. **Don't unanchor** — the manifest disappearing from git is a silent failure mode (the PHP shell skips the script tag, page loads, nothing happens).
5. **`render_shell()` skips both `<script type="module">` and `<link rel="stylesheet">` tags when the manifest is missing.** Dev environments without a build still render the install-prompt cleanly. The composer route renders an empty `<main id="outpost-root">` until the build exists — visible failure mode is "page loads but nothing happens," which is debuggable from the browser console.
6. **Auth flow orchestration in `pwa/src/lib/auth-flow.ts`.** Two public functions: `begin_login(me, client_id, redirect_uri, scope?)` and `handle_callback(query, client_id, redirect_uri)`. State persists in `sessionStorage` under keys `outpost.auth.{verifier, state, me, token_endpoint}`. sessionStorage was chosen deliberately over localStorage: a stale verifier from yesterday's abandoned flow shouldn't validate today's callback, and same-tab persistence across the auth-endpoint redirect is exactly what sessionStorage provides.
7. **`AuthFlowError` discriminates failure paths.** Codes: `state_mismatch`, `missing_state`, `exchange_failed`, `no_code`. The UI surfaces a different message per code so the user can pick the right recovery action. `handle_callback` always clears sessionStorage in `finally` — a failure must not leave orphaned state for the next attempt to inherit.
8. **Three Preact components for the user-facing surfaces.** `LoginScreen` (single form, `me` URL pre-filled with `location.origin + '/'`, submits via `begin_login`, navigates same-tab via `location.assign`). `AuthCallback` (mounts on `/post/auth/callback`, runs `handle_callback`, redirects via `location.replace` so the back button doesn't replay the consumed code). `ComposerPlaceholder` (logged-in state for B0b, shows `me` + scope, "Sign out" button that calls `clear_token` and reloads).
9. **Mount logic in `index.tsx`'s `App` component reads `detect_route(location.pathname)` once.** For `composer` route, calls `read_token` to choose between `LoginScreen` and `ComposerPlaceholder`. The route doesn't change without a navigation, so no `popstate` listener — a successful login does `location.replace('/post/')` which remounts the whole app fresh.
10. **`mount(root, props)` exported separately from the auto-mount block.** Top-level auto-mount only runs when `document.getElementById('outpost-root')` exists. The test-time module import doesn't accidentally mount because happy-dom's document doesn't have that element. Lets B0c+ tests assert the component tree against an injected root if needed without re-running auto-mount.
11. **CSS in the bundle for B0b; A3 will move tokens to server-rendered.** `pwa/src/styles/structure.css` ships in the Vite build. Per A3 design constraint #1, `styles/outpost-tokens.css` will be server-rendered and the structural CSS may relocate. Structure is plugin territory; color/font/radius/shadow tokens become theme-overridable via `var(--outpost-*, neutral-fallback)`.
12. **`OUTPOST_VERSION` bumped to `0.1.2`.** A2 Locked Decision #16: any deployable behaviour change moves the version, even when it's not a rewrite-table change. B0b ships the JS bundle enqueue + the login UI + the manifest reader — all observable changes.

## Session B1 — Locked Decisions (shipped)

Closed 2026-05-01. The constraints below are now production code; the rationale stays here so future sessions don't re-litigate. B1 lands the Micropub client + a minimal note-posting form, replacing B0b's `ComposerPlaceholder`.

1. **Per-post discovery, not persistent in token meta.** `discover_micropub_endpoint(me)` runs each fresh post; `NoteForm` caches the result in component state for the session. IDB schema unchanged — keeping it stable matters more than saving one HTTP request per post. B1c+ may persist `micropub_endpoint` in `StoredToken.meta` if discovery latency becomes a measurable UX issue.
2. **Form-encoded body, not JSON.** `h=entry&content=<text>`. Simplest valid Micropub request; every server accepts it. JSON Micropub (richer microformat shapes for replies/photos/articles) lands when Phase C's modes need it.
3. **201 Created and 202 Accepted both treated as success** per the Micropub spec. UI shows the same "Posted to: <url>" message for both — the async-vs-sync distinction isn't user-facing.
4. **`MicropubError` codes:** `discovery_failed`, `no_endpoint`, `post_failed`, `no_location`. Mirrors the `AuthFlowError` pattern from B0b. UI prefixes message with the code (`<code>: <message>`) so screenshot triage from staging is fast.
5. **`MicropubEnvironment { fetch }` is minimal.** No `crypto`/`random`/`indexedDB`. The Micropub client doesn't need them; fewer test stub surfaces is cheaper to maintain. Compare `IndieAuthEnvironment` (fetch + crypto + random) and `TokenStoreEnvironment` (indexedDB + crypto) — each env exposes only what its client actually consumes.
6. **`micropub.ts` reuses parse helpers from `indieauth.ts`** rather than re-implementing DOM/Link-header parsing. `parse_link_header` and `parse_html_endpoints` are exported from indieauth specifically so micropub can import them. Discovery shape is identical between the two specs.
7. **`composer-placeholder.tsx` deleted in this commit, not just unimported.** Replaced by `note-form.tsx`. Recoverable from `0cc82c9` if a future need re-emerges. Reducing surface area is the right call when "is there still a use for this?" is "no, never again."
8. **Hard Contract verified for B1 CSS.** `.outpost-textarea` paint fallbacks: `background: var(--outpost-input-bg, transparent)`, `color: var(--outpost-input-fg, inherit)`, `border: 1px solid var(--outpost-border, currentColor)`. **Not `revert`** — `revert` for form inputs would force UA defaults that ignore the theme (opposite of what we want for inputs). The B0b button hotfix used `revert` correctly because BOTH `background` and `color` collapsed to the same value; for textareas the two properties have different fallbacks (`transparent` vs `inherit`) so they don't collapse. The `revert` rule is for elements where same-property collapse is the failure mode, not for all paint fallbacks.
9. **`OUTPOST_VERSION` bumped to `0.1.4`** per A2 Locked Decision #16 (any deployable behaviour change moves the version). B1 ships a JS bundle change — observable behaviour on `/post/`.

## Session C0 — Locked Decisions (shipped)

Closed 2026-05-01. Phase C kickoff: tab framework first, Note plugged in, four stubs for the remaining modes. Establishes the design pattern explicitly before any of the bigger modes (Reply, Photo, Listen group, Article) lands.

1. **Tab framework first, modes second.** C0 ships the WAI-ARIA tabs scaffold with Note as the only real mode; Reply, Photo, Listen, and Article render placeholder cards naming which Phase C session lands the real implementation. Honest WIP indicator. No tabs hidden — the user sees all five modes are coming.
2. **WAI-ARIA tabs pattern (APG).** `role="tablist"` on the container with `aria-label="Composer modes"`. Each tab: `role="tab"`, `aria-selected`, `aria-controls`, roving tabindex (selected=0, others=-1). Each panel: `role="tabpanel"`, `aria-labelledby` pointing back at the tab. Closes A11Y-CHECKLIST Phase C focus-management forward gate.
3. **Automatic activation.** Arrow keys move focus AND switch the active tab in one keystroke. Appropriate because tab switches are cheap (toggle visibility on already-rendered panels). Manual activation (focus only, Enter/Space activates) is for tabs with side effects.
4. **All panels rendered eagerly; visibility toggles via the `hidden` attribute.** Keeps mode state across tab switches — text typed into Note's textarea persists when the user clicks Reply and back. Inactive panels are `display: none` via the UA stylesheet's `[hidden]` rule (and explicit fallback in `structure.css`).
5. **Wrap-around at the ends.** ArrowRight from last tab → first; ArrowLeft from first → last. Home/End jump unconditionally to first/last. The APG explicitly allows either wrap or stop-at-end; wrap is more keyboard-friendly for mobile/desktop both.
6. **Heading hierarchy: panel titles are `<h2>`, not `<h1>`.** B1's NoteForm used `<h1>` (page-level). With panels nested under the tablist, the panel headings drop to `<h2>`. The tablist's `aria-label` provides the page-level structural label; no separate `<h1>` is needed because the tablist IS the primary content. If a future surface (Phase H settings, B2 install-prompt) needs a page heading, it adds its own `<h1>`.
7. **Mode files live at `pwa/src/components/modes/*-mode.tsx`.** Clean separation: ComposerTabs is the framework; modes/ contains the per-mode components. Naming convention `<kind>-mode.tsx` matches IndieWeb post-kinds vocabulary.
8. **Stubs name their landing session.** Each placeholder card includes a one-line lede mentioning which Phase C session lands the real mode (`"Reply, Like, Repost… land in Phase C1"`). Future contributors see the placeholder and the timeline, not just "TODO."
9. **Bundle budget.** Phase C0 bundle: 30.71 KB / 10.84 KB gzipped (up from 27.88 / 10.00 at v0.1.8). PERFORMANCE-CHECKLIST.md sets a 40 KB gzipped ceiling for the entry through Phase C; current usage is 27% of budget. Future modes that exceed this budget code-split via dynamic `import()`.
10. **`exactOptionalPropertyTypes: true` requires conditional spread.** Passing `micropubEnv={micropubEnv}` to NoteMode when `micropubEnv` is `MicropubEnvironment | undefined` fails type-check under the strict ceiling. ComposerTabs spreads conditionally: `{...(micropubEnv ? { micropubEnv } : {})}`. Pattern reusable for any optional env that flows through wrapper components.

## Session B2 + C1 — Locked Decisions (shipped)

Closed 2026-05-01. Phase B2 (server-side mf2 preview endpoint with SSRF defenses) lands together with Phase C1 first variant (Reply mode). C1's other variants (Like, Repost, Bookmark, RSVP, Follow) extend Reply with a sub-mode picker in C1b — all share the same form shape and the same B2 endpoint.

1. **B2 endpoint at `/wp-json/outpost/v1/preview`.** POST with `{ url }` body returns `{ html, finalUrl, contentType }`. `Outpost_Preview_Endpoint` is `final`, static-only, `register()` hooks `rest_api_init` to register the route. `show_in_index => false` keeps it out of `/wp-json/`'s public route index — closes the AI Engine CVE-2025-11749 vulnerability class. Per PHP-SURFACE-CHECKLIST.md.
2. **Permission via the IndieAuth plugin's REST middleware.** `permission_callback` checks `current_user_can( 'edit_posts' )` — no custom bearer-token validation. The IndieAuth plugin's `authenticate` filter translates `Authorization: Bearer ...` headers into a WP current user before the permission callback runs, so the standard cap check works for both cookie auth (admin) and bearer auth (PWA).
3. **SSRF defenses.** Scheme allowlist (http/https only, before host check so `file://` gets `invalid_scheme` not `invalid_url`). `wp_safe_remote_get` for the fetch — auto-blocks loopback + private network ranges via WP's `http_request_host_is_external` filter chain. 3-second timeout. 5 MB response-size cap (post-fetch length check; not true streaming, but adequate for typical IndieWeb post sizes).
4. **Content-Type allowlist.** `text/html` and `application/xhtml+xml` only. Charset suffixes (`text/html; charset=utf-8`) accepted via `strpos === 0` prefix match. Rejects JSON, images, PDFs, anything else with a 415.
5. **Per-user rate limit, 30 requests/minute.** Transient-backed counter keyed on `outpost_preview_rl_{user_id}`. Returns 429 with `retryAfter: 60` when exceeded. Defends against accidental burst (autocomplete fires on every keystroke) and intentional abuse.
6. **Script + iframe stripping in returned HTML.** Defense in depth: the client never executes returned HTML (it parses for `<title>` via regex and forwards to microformats parsers when needed), but a future code path that naively renders shouldn't be a security regression. Strips `<script>`, `<iframe>`, `<object>`, `<embed>` blocks; `on*=` event handlers; `javascript:` / `data:` href and src.
7. **Client `extract_title()` via regex, not DOMParser.** Page titles are simple — a regex match plus entity decoding (named entities + numeric character references via pure string replace) covers 99%+ of real-world cases. Avoids the `innerHTML` hook flag and any DOM dependency. Richer mf2 parsing (h-card, h-entry properties beyond name) lands when other reply variants need it.
8. **`PreviewError` codes mirror the response shape.** `unauthorized` (401/403), `invalid_url` (client-side scheme check), `unsupported_content_type` (415), `rate_limited` (429), `server_error` (5xx), `fetch_failed` (network). UI surfaces `code: message` for screenshot triage, same as `MicropubError` and `AuthFlowError`.
9. **`post_h_entry({ properties, accessToken, micropubEndpoint })` is the new general-purpose Micropub poster.** Form-encodes h-entry properties from a `HEntryProperties` interface (content, name, summary, in-reply-to, like-of, repost-of, bookmark-of, category, mp-syndicate-to). Arrays get `[]` suffix per the Micropub spec. `post_note` is now a thin wrapper that calls `post_h_entry({ properties: { content } })` — preserves backward compat with B1 callers.
10. **Reply mode props don't carry `tokenStore`.** Sign-out is global (the page reloads on Sign Out, unmounting all modes). Only NoteMode needs `tokenStore` because it owns the Sign Out button. ReplyMode's props are `{ token, micropubEnv? }` — minimum it needs to discover the endpoint and post.
11. **Optional preview step in Reply mode.** User pastes target URL, optionally clicks "Show preview" to fetch citation context (page title + final URL after redirects). Preview is informational — submitting doesn't require previewing. Keeps the form usable when staging is slow or the target fails preview.
12. **`OUTPOST_VERSION` bumped to `0.1.10`.** Per A2 #16 — both PHP (REST route registration) and JS (Reply mode + post_h_entry refactor) ship deployable behavior.

## Session C1b — Locked Decisions (shipped)

Closed 2026-05-02. Adds three reply variants — Like, Repost, Bookmark — to the existing Reply mode under a single tab. RSVP and Follow defer to a future C1c (RSVP needs an extra `rsvp:` yes/no/maybe control; Follow's spec is contested across servers).

1. **One tab, four variants via a radio picker.** Reply, Like, Repost, Bookmark all share the same form shape (URL + optional content + target-property name); a single component handles all four. Tab label stays "Reply" as the umbrella; the visible affordance is the radio group at the top of the form.
2. **`VARIANTS: Record<Variant, VariantConfig>` is the table-data source.** Each entry maps a variant id to its label, target h-entry property name (`in-reply-to` / `like-of` / `repost-of` / `bookmark-of`), whether content is required, target-input label, content-textarea label, submit button label, and preview-intro string. Adding RSVP or Follow in C1c is a single-row addition (plus any UI quirks they need beyond the shared shape).
3. **Content-required only for Reply.** Reply needs both URL and content; Like, Repost, Bookmark only require URL. The `contentRequired` boolean per variant drives the submit-disabled logic and the `required` attribute on the textarea.
4. **`<fieldset>` + `<legend>` + `<label>`-wrapped radios** for a11y. Browser-native arrow-key navigation between radios in the same `name` group; Tab moves between fieldsets. Generous padding (`0.375rem 0.625rem`) on each `<label>` so the touch target is comfortable on mobile.
5. **`:has(input:checked)` for selected-state styling.** Modern selector, supported in iOS Safari 15.4+ and Chrome 105+. Outpost's existing browser baseline (the PWA already requires modern fetch/IDB/crypto.subtle) accommodates it. The pattern lets the visual state of the parent `<label>` reflect the radio's checked state without JS.
6. **Submit button label per variant.** "Post reply" / "Post like" / "Post repost" / "Post bookmark". The "Finding endpoint…" and "Posting…" transient labels still override during submission. The button stays a single primary action — switching variant changes its label, not its position.
7. **Property assignment uses computed key.** `properties[config.property] = trimmed_url` would be a TypeScript error under `exactOptionalPropertyTypes`; the working pattern is `{ [config.property]: trimmed_url, ...(content ? { content } : {}) }` (computed property in literal + conditional spread). Captured here because future modes that build dynamic `HEntryProperties` shapes will hit this same constraint.
8. **State preservation across tab switches.** Variant selection, target URL, content text, and component-cached endpoint URL all survive when the user clicks Note → Reply → Note again, courtesy of the C0 eager-render-and-hide pattern. Going across the whole composer round-trip without losing form state is the load-bearing UX win of the tab framework.
9. **7 new vitest tests for ReplyMode.** Component-level tests covering: 4 radios in order, Reply default, heading updates on variant change, submit-label updates per variant, Reply requires both URL+content, Like requires URL only, target-input label updates per variant. First component test for a mode (NoteMode/AuthCallback are still untested at the component level — covered by integration via micropub.test.ts and auth-flow.test.ts respectively). Vitest 115 → 122.
10. **`OUTPOST_VERSION` bumped to `0.1.11`.** Per A2 #16 — JS bundle change + new behavior (3 new post kinds posting through the same form). Bundle: 36.38 KB JS / 12.34 KB gzipped (was 35.23 / 11.97 — +0.37 KB gzipped). 31% of the 40 KB Phase C budget.

## Session F1 — Locked Decisions (shipped)

Closed 2026-05-04. First Phase F deliverable. Adds the outbound `?q=syndicate-to` chip surface, with ActivityPub as the single shipping adapter. Detection + chip surfacing only — no publishing logic in F1; the Pfefferle/Automattic ActivityPub plugin handles federation on its own `transition_post_status` hook.

1. **`Outpost_Companion_Base` extends with `syndicate_chip(): ?array`, default null.** The new method declares the chip-shape contract (`id`, `label`, `accepts`, `detected`) every adapter that contributes a syndication target overrides. Adapters that don't surface a chip (Post Kinds, Post Formats, XFN, Yoast, Accessibility Checker, Syndication Links) keep the default null; adapters that do (ActivityPub now, Bridgy Publish in F14, ManualShare in F9) override. The base remains abstract for the original three methods (`file()`, `label()`, `capabilities()`).
2. **The chip shape mirrors what the F5 inbound `Outpost_Source_Base` will need.** Fields are deliberately bidirectional: `id`, `label`, `accepts`, `detected` all carry over with the same semantics; F5's source adapters add `extractor` (oembed, og, mf2, rss, api) and `host_patterns`. The composer should iterate either family with a uniform vocabulary instead of branching on direction. Captured in the base's class-level docblock.
3. **The Shanske Micropub plugin's `micropub_syndicate-to` filter is the single integration point.** Outpost does NOT host its own `?q=syndicate-to` route. The Micropub plugin owns Micropub server semantics; Outpost contributes chips through the documented filter `(array $targets, ?int $user_id) => array` where each target is `[ 'uid' => string, 'name' => string ]`. The original prompt referenced a non-existent `Micropub_Server.php` — the actual home is `Outpost_Micropub_Bridges`, which already pairs the read-side filter with the write-side `after_micropub` action.
4. **`Outpost_Micropub_Bridges::merge_syndicate_chips()` projects the rich chip shape down to `[ uid, name ]`.** Adapters declare in the four-key shape; the merger projects `id` → `uid` and `label` → `name` for the filter consumer. De-duplication is by `uid`: if a chip with the same uid already exists in `$targets` (from another filter caller), the merger keeps the existing entry and skips the companion's. Existing chips win — adapters never overwrite a pre-registered destination.
5. **Naming divergence from the F1 prompt is intentional.** The prompt requested `Companion_Base.php`, `Companion_ActivityPub.php`, `Micropub_Server.php`, `tests/companions/Companion_ActivityPubTest.php`, `tests/fixtures/activitypub-plugin-fixture.php` — but the repo's pre-existing convention is `class-companion-base.php` (kebab-case files), `Outpost_*_Adapter` class names, all tests in `tests/unit/`, no fixtures directory. Following the existing convention preserves the eight already-shipped adapters, the Companion_Registry contract, and the existing test suite organization. Future Phase F prompts should expect `class-*-adapter.php` filenames.
6. **§5 posture preserved.** Detection is `is_plugin_active()` only. No calls into ActivityPub plugin internals, no embedded credentials, no external API calls. The chip is a pure intent signal — the user's "yes, federate this" toggle.
7. **i18n boundary.** The chip label `__( 'Fediverse (via ActivityPub plugin)', 'outpost' )` is wrapped per the project's i18n contract. The wrapping is safe because `merge_syndicate_chips()` runs as a filter callback on `micropub_syndicate-to`, which fires during request handling — well after `init`, so the textdomain JIT trap (covered separately) doesn't apply.
8. **Test posture: `WP_Mock::userFunction('is_plugin_active')` + `userFunction('get_plugins')` are both required for inactive-state tests.** When `is_plugin_active()` returns false, `Outpost_Companion_Detector::status()` falls through to `get_plugins()` to distinguish 'inactive' from 'absent'. Tests that exercise the inactive path must stub both. Active-state tests only need `is_plugin_active`.
9. **Generic test fixture values only.** The two research wiki documents (`concepts/posse-outbound-may-2026.md`, `concepts/capture-inbound-may-2026.md`) are keyed to a specific user's case-study handles. NONE of those handles appear anywhere in code or test fixtures; the only example values used are `example.social`, `example.com`, and `manual-mastodon-example-social`. Acceptance criterion grep confirms zero leakage.
10. **Tests added: 9.** PHPUnit count 128 → 137. New tests cover `Outpost_ActivityPub_Adapter` shape (file/label/capabilities/active+inactive `syndicate_chip()` returns), `Outpost_Companion_Base` default-null chip behavior, and `Outpost_Micropub_Bridges::merge_syndicate_chips()` across five paths (active append, inactive omit, existing-passthrough, dedupe-by-uid, non-array input). No version bump — F1 is a static scaffold + filter registration; no observable behavior change without the user installing a Micropub client and the ActivityPub plugin together.

## Session F2 — Locked Decisions (shipped)

Closed 2026-05-04. Builds on F1. Defines the `capabilities()` contract on `Outpost_Companion_Base`, implements it for ActivityPub with the conservative shape, adds `chips_for_mode()` on the registry, and exposes a per-mode chip listing endpoint at `/wp-json/outpost/v1/syndicate-targets`.

1. **`capabilities()` reclaimed for the chip shape; old slug accessor renamed `feature_slugs()`.** F1 introduced `syndicate_chip()` as the chip-shape method, but the F2 prompt called the same surface `capabilities()` — already taken by the slug-based method on `Outpost_Companion_Base` (used by all 8 adapters). Resolution: rename the slug accessor to `feature_slugs()` (8 adapter methods + base abstract + registry's `all_active_capabilities()` → `all_active_feature_slugs()` + 6 test assertions) and replace `syndicate_chip()` with `capabilities()` returning the richer F2 shape. Net effect — clearer naming (slugs vs chips don't share a method name) and `capabilities()` matches the contract every Phase F prompt will reference.
2. **`capabilities(): ?array` is concrete with default null, not abstract.** The F2 prompt specified abstract — but the 8 existing feature-surface adapters (Post Kinds, Yoast, XFN, Post Formats, Syndication Links, Accessibility Checker) don't surface syndicate-to chips and shouldn't be forced to declare an empty shape. Concrete-default-null lets feature-surface adapters keep doing what they do; syndication-target adapters (ActivityPub now, F9 ManualShare, F14 BridgyPublish) override. The two adapter categories are explained in the base class's class-level docblock.
3. **The full F2 chip shape is `{id, label, detected, accepts_modes, accepts_media, max_attachments, alt_passthrough, char_limit, caveats, requires_auth}`.** Documented in `Outpost_Companion_Base::capabilities()`'s docblock. Adapters that only need a subset (e.g. F1's old four-key shape) supply null/empty-array values for the unused keys.
4. **F2 → F5 symmetry remains.** The inbound `Outpost_Source_Base` planned for F5 mirrors capabilities() inverted: same field names — `id`, `label`, `detected`, `accepts_modes` (which composer modes the source can extract into), `caveats` — plus an `extractor` key naming the metadata recipe and `host_patterns` for host-match dispatch. Captured in the base's docblock so the F5 author can build on this without re-deriving the shape.
5. **`Outpost_Companion_Registry::chips_for_mode(?string $mode)` is the per-mode filter.** Iterates active companions, calls each `capabilities()`, returns those where `detected === true` AND the requested mode appears in `accepts_modes`. Pass null (or omit) to get every detected chip without filtering. The composer hits this for its syndication strip; `Outpost_Micropub_Bridges::merge_syndicate_chips()` calls it with null because the Shanske `micropub_syndicate-to` filter has no per-mode parameter.
6. **Mode validation is fail-OPEN.** Unknown modes return every detected chip rather than zero. The Outpost composer always sends a known mode; defensive callers (third-party Micropub clients passing unknown modes through future filter extensions) get the full set so a typo doesn't silently hide all destinations. Codified in the registry's `is_known_mode()` private helper plus the `outpost_known_composer_modes` filter so site owners can extend the recognized set.
7. **`/wp-json/outpost/v1/syndicate-targets?mode=...` is the new Outpost-owned endpoint.** The F2 prompt asked to extend the Shanske Micropub plugin's `?q=syndicate-to` handler with `&mode=`, but the Shanske filter contract has no mode parameter and Outpost can't change Shanske's plugin. Instead, Outpost ships its own REST route mirroring the security posture of preview/composer-config endpoints (cookie OR `edit_posts` cap OR bearer presence; `show_in_index => false`; static-data only with no SSRF surface or rate limiting needed). Composer reads from this endpoint when rendering per-mode strips; Shanske's `?q=syndicate-to` continues to serve all-modes calls for any third-party Micropub client.
8. **Filter `outpost_companion_capabilities` is the per-companion override hook.** Signature `(?array $caps, string $companion_id) => ?array`. Site owners can: (a) replace any capability key, (b) extend `caveats`, (c) restrict `accepts_modes` per a federation policy, (d) return null to force-hide a chip even when the underlying plugin is active. Applied inside `Outpost_ActivityPub_Adapter::capabilities()` before the return; future syndication-target adapters apply it the same way. The companion ID is passed so callers filtering on multiple companions can dispatch.
9. **Test fixture as a real file at `tests/fixtures/companion-restricted-modes.php`.** The fixture defines `Outpost_F2TestRestricted_Adapter` matching the registry's `^Outpost_[A-Z][A-Za-z0-9_]*_Adapter$` regex gate so tests can register it via the `outpost_companion_adapters` filter. The fixture's chip declares `accepts_modes => ['photo']` so per-mode filtering can be proven without depending on F9's ManualShare. Generic example-only values throughout — no real plugin file path, no instance handles, no case-study identifiers.
10. **`WP_Mock::onFilter()` requires `withAnyArgs()` not `with()`.** Discovered during F2 test development: `WP_Mock\Functions::type('array')` matchers passed to `with()` don't intercept the filter, leaving the default passthrough behavior. `withAnyArgs()` is the working form. Captured here so future tests setting up filter mocks default to the working pattern. Pairs with the existing CLAUDE.md A2 #8 workaround (`Filter::$filtersWithAnyArgs` static-state reset in `setUp()`).
11. **Tests added: 14.** PHPUnit count 137 → 151. New tests cover the rename (existing 4 adapter-shape tests), capabilities() shape with all 10 required keys, default-null when plugin inactive, the `outpost_companion_capabilities` filter (replace + force-null), `chips_for_mode()` across five paths (photo, note, restricted-fixture-excluded-on-note, restricted-fixture-included-on-photo, undetected-companions, unknown-mode-fail-open, null-returns-all), `known_modes()` shape, and the new endpoint across four paths (photo, unknown-mode-fail-open, no-mode, restricted-fixture-filtering). No version bump — F2 is contract evolution + new endpoint with no observable behavior change for users not running both Micropub + ActivityPub plugins.

## Session F3 — Locked Decisions (shipped)

Closed 2026-05-04. Verifies image alt-text passthrough Outpost → Micropub plugin → WP attachments → ActivityPub plugin transformer. Investigation surfaced an upstream Micropub-plugin gap; F3 ships the Outpost-side fix.

1. **Investigation finding: Outpost is a Micropub CLIENT.** No PHP photo upload handler exists in the Outpost repo — the prompt referenced `Micropub_Server.php`, but image uploads land at the Shanske Micropub plugin's `/wp-json/micropub/1.0/media` endpoint. Outpost's photo path is purely client-side (`pwa/src/lib/photo.ts` for processing, `pwa/src/lib/micropub.ts` for upload + create-post requests). What Outpost owns is the SHAPE of the Micropub create request and the `after_micropub` bridge hook.
2. **Investigation finding: the AP plugin reads `_wp_attachment_image_alt` for `attachment[].name`.** Confirmed against `Automattic/wordpress-activitypub` — `class-base.php::transform_attachment()` calls `get_post_meta( $attachment_id, '_wp_attachment_image_alt', true )` and stores it (HTML-stripped + entity-decoded) at `attachment[].name`. The transformer also runs `do_shortcode($content)` on post_content before parsing img tags, so the Micropub plugin's `[gallery]` shortcode output works correctly.
3. **Investigation finding: the upstream Micropub plugin does NOT write `_wp_attachment_image_alt`.** Confirmed against `indieweb/wordpress-micropub`. Two failure modes:
   - Structured photo entries `{ value, alt }` are recognized, but the alt is passed to `media_sideload_url($url, $post_id, $title)` which stores it as `post_title` rather than `_wp_attachment_image_alt`.
   - The parallel `mp-photo-alt` array convention isn't honored at all — Outpost client requests using that shape silently drop alt text.
   Net effect: every Outpost-originated Photo or Gallery post syndicated via the AP plugin federates with empty alt text. Accessibility regression for every user. Filed as a follow-up to consider an upstream PR; Outpost ships the fix in-tree below.
4. **Outpost-side bridge: `Outpost_Micropub_Bridges::apply_photo_alt_text()`.** Hooks the existing `after_micropub` action at priority 20 (alongside the other bridges) and writes `_wp_attachment_image_alt` for every photo URL in the request that resolves to an attachment via `attachment_url_to_postid()` and is parented by the post being created (or has post_parent=0). Reads BOTH photo shapes — structured `{ value, alt }` AND parallel `mp-photo-alt` arrays — so legacy clients work too. The structured shape wins per-entry when both are present (matches the Micropub spec's stated precedence).
5. **Empty alt strings are persisted, not skipped.** AP spec accepts empty `attachment[].name`; some downstream AP consumers default to URL/filename when the field is missing entirely. An explicit empty value beats a missing field.
6. **Defensive parent check.** The bridge skips attachments whose `post_parent` resolves to a different post — defends against URL collisions or third-party media libraries shared across posts. Attachments with `post_parent = 0` (orphan during early Micropub processing) are accepted.
7. **Locked: `_wp_attachment_image_alt` is the canonical alt-text storage.** Any future Outpost code that touches image alt text writes there, not to a custom Outpost meta key. The AP plugin (and WordPress core's `wp_get_attachment_image()`) both read from this key; alternative keys would require fork-style coordination.
8. **Locked: gallery posts emit one attachment per image, not a composite.** The Micropub plugin already does this correctly via `media_handle_upload`/`media_sideload_url` per photo entry; F3 doesn't change it but locks the contract.
9. **Locked: post_content for Photo posts is the `[gallery size=full columns=1]` shortcode the Micropub plugin emits.** Outpost does NOT override this. The AP plugin's `do_shortcode()` call renders it, the rendered img tags then get parsed. Future write-shape sessions that consider switching to `core/image` blocks must verify the AP transformer still finds the attachments via `attachment_url_to_postid()` on the rendered URLs.
10. **Companion_ActivityPub caveats array gets a second entry** noting that alt-text passthrough requires Outpost active alongside the AP plugin. Without the bridge, even a correct Outpost client request loses alt text upstream.
11. **Test fixtures: `tests/fixtures/sample-photo-200x200.jpg` and `-alt.jpg`.** Generated test patterns via PHP/GD (gray + blue solid backgrounds with a stripe and circle), ~2 KB each. Generic, no personal photos. Reserved for the wp-env-backed integration tests when those land.
12. **Integration test stubs follow the A2 #9 pattern.** `tests/integration/CompanionActivityPubPassthroughTest.php` (4 stubs: single-photo, gallery, no-alt, regression-without-bridge) and `tests/integration/MicropubPhotoWriteShapeTest.php` (2 stubs: alt-persistence-without-AP, parallel-array-shape) document the wp-env assertions the future session will flesh out. Each stub's docblock spells out the exact steps. They run via `markTestSkipped` today.
13. **Tests added: 8 unit + 6 integration stubs.** PHPUnit count 151 → 165 (with 6 skipped integration tests). Unit tests cover the bridge across 7 paths (structured shape, parallel-array shape, empty-alt-persists, foreign-parent-skipped, unresolvable-URL-skipped, no-photo-noop, orphan-attachment-accepted) and a direct `collect_photo_alt_pairs()` resolver test. No version bump — F3 ships defensive server-side behavior that activates on the next photo post; existing Outpost installs benefit immediately on plugin update without any user action.
14. **Follow-ups deferred to later sessions:**
    - Client-side update: switch `pwa/src/lib/micropub.ts` photo encoding from parallel `mp-photo-alt` arrays to structured `{ value, alt }` JSON objects, with `application/json` content-type. The bridge handles both shapes today, so the client switch is non-blocking.
    - Upstream PR to `indieweb/wordpress-micropub` proposing `media_sideload_url`/`media_handle_upload` write `_wp_attachment_image_alt` from the structured photo property's `alt` field. Not load-bearing for Outpost users (the bridge handles it) but reduces fediverse-wide accessibility regressions for non-Outpost Micropub clients.
    - Bluesky-via-Bridgy-Fed image down-rezzing — the caveat in the ActivityPub adapter mentions the limits but doesn't enforce them. Phase G concern.
    - Video / audio attachment passthrough — same chain as images but the Micropub plugin's gallery shortcode covers `video` and `audio` fields too. F3 scope is image-only; future session covers video alt-equivalents (description) and AP attachment.mediaType handling.

## Session F4 — Locked Decisions (shipped)

Closed 2026-05-04. Two-part deliverable: raise Companion_ActivityPub coverage with edge-case tests, ship the §5 audit lint as a required CI gate. The lint is the load-bearing piece — every Phase F session after F4 inherits automated detection of case-study leakage, embedded credentials, hardcoded silos, untranslated strings, and personal-data fixture leaks.

1. **`bin/lint/section-5-audit.sh` is a required CI check on main.** Five sub-checks (B1–B5) run as the new `lint-section5` job in `.github/workflows/ci.yml`. Job runs in <1 second against the current source tree (well under the 30s ceiling). Required-status configuration on the main branch is the repo admin's job; F4 ships the job itself.
2. **Configuration files own the authority, not the bash script.** Forbidden tokens, credential regexes, instance allowlists, and fixture-handle allowlists each live in their own sibling `.txt` file under `bin/lint/`. Updates to any list require a session-log entry explaining the change. Keeps the lint's audit trail auditable by the same review process as code changes.
3. **B1 token list intentionally omits bare `courtneyr`.** The prompt listed it; the GitHub repo's organization is `courtneyr-dev/outpost` which legitimately appears in package metadata, the About-tab bookmarklet source link, and the bundle's source-map comments. Bare `courtneyr` would self-flag the project's own canonical references. The narrower tokens (`courtneyr.dev`, `m.courtneyr`, `courtneyr_dev`, `courtneyengle`, `courane`, `streamlining`, plus the three opaque IDs from the research doc) match case-study-specific patterns that cannot collide with legitimate project metadata.
4. **B3 instance allowlist includes Bridgy bridge endpoints (`brid.gy`, `bsky.brid.gy`).** These appeared in `class-composer-config-endpoint.php` from earlier sessions. They're protocol bridges (Ryan Barrett's Bridgy Fed), not user-specific instances — no leak. Documented in `instance-allowlist.txt` so future sessions know why they're there.
5. **B4 i18n check is grep-based, not AST-based.** Two patterns: `'label' => 'string'` and `'caveats' => array('string')` inside `includes/companions/`. Anything matching that's NOT preceded/followed by `__(`, `_x(`, `_n(`, `esc_html__(`, or `esc_attr__(` fails. Heuristic, not exhaustive, but adequate — the AP adapter's full capabilities() shape passes today and any future companion that adds bare strings will trip the check at PR time.
6. **B5 fixture-handle allowlist uses cryptography stock names.** alice / bob / charlie / dave / eve / mallory / trent / walter / peggy / victor — RFC 4949 / NIST examples. Any other handle in `tests/fixtures/` triggers a B5 failure (with PHPDoc tags `@param` / `@return` / etc. excluded via the leading-`*` line filter).
7. **The lint excludes its own config files via `--exclude-dir=lint`.** The token list, credential patterns, and instance allowlist all contain by design the very strings they're meant to detect; without the exclusion the lint would self-flag.
8. **Suppression markers, deliberately narrow.** B1 exempts lines that mention either research-doc filename. B2 exempts lines tagged `outpost-lint:fixture-credential`. No global "ignore this file" mechanism — every suppression carries justification on the same line.
9. **Coverage floor for Phase F adapters: 85% line coverage, per-class.** Aggregate coverage is too easy to game by adding tests for happy paths in well-tested code while leaving untested branches in adapter classes. Phase F sessions verify their target adapter individually. Coverage tooling (Xdebug / pcov) lands in a future Phase G session — F4 verifies coverage by manual gap analysis (every branch in `Outpost_ActivityPub_Adapter::capabilities()` is now exercised: active+filter-passthrough, active+filter-replace, active+filter-null, active+filter-non-array-fallback, active+filter-partial-shape, inactive-short-circuit, caveats-shape).
10. **Tests added: 17.** PHPUnit count 165 → 182. Two new test files: `CompanionActivityPubFilterTest.php` (8 tests covering filter edge cases — non-array fallback, integer fallback, partial shape, caveats-extension, single-key modification, inactive-short-circuit, caveat shape integrity) and `CompanionLoaderEdgeCasesTest.php` (10 tests covering zero-companion paths, malformed capabilities, empty-string mode, null mode, detection consistency, known-modes filter extension + fallback). All 7 integration test stubs continue to skip pending wp-env.
11. **`composer lint:section5` runs the audit locally.** New script entry in `composer.json` plus a `lint:all` meta-script that runs both `phpcs` and `lint:section5`. The existing `check` meta-script also gains `@lint:section5` so `composer check` is the single full-suite invocation.
12. **Demonstrated failure modes (per F4 acceptance criterion 5).** Three throwaway demonstrations confirmed the lint catches: (1) hardcoded `courtneyr.dev` URL in `tests/__lint_demo.php` → B1 file:line:violation. (2) `$api_key = 'sk_live_...';` matching the pattern → B2 file:line:violation. (3) `https://mastodon.notallowlisted.example/` URL → B3 file:line:violation. Demo output preserved in the F4 PR description; demo file never committed to main.
13. **CONTRIBUTING.md documents the lint.** New section after "Forbidden vocabulary" covers: how to run locally, the four config files and their purposes, the two suppression markers (research-doc citation for B1, `outpost-lint:fixture-credential` for B2), how to add a new fixture-handle name, and how to add a new allowed instance hostname. README.md gets a one-line mention of `composer lint:section5` in the development commands.
14. **Self-test passing.** `bin/lint/section-5-audit.sh` exits 0 against the current commit. Every Phase F session before F4 was clean enough that the lint surfaced zero violations on first run — F1, F2, F3 were already case-study-clean (the F1/F2/F3 prompts each had their own grep verification). F4 just makes it automatic.

## Session F5 — Locked Decisions (shipped)

Closed 2026-05-04. Inbound source-adapter foundation. Defines `Outpost_Source_Base` (mirrors F1+F2's `Outpost_Companion_Base` inverted), the registry, the URL-pattern matcher, the seven extractor types (one concrete, six stubs), and extends the B2 preview endpoint with optional `source_id` dispatch. F7 lands the first concrete source (Spotify); F6 wires the dispatcher + mode-picker UI; F16 lands the og_tags parser to make `Source_Unknown` end-to-end.

1. **`Outpost_Source_Base` mirrors `Outpost_Companion_Base` inverted.** Class-level docblock cites the F1+F2 companion shape and lays out the parallel keys explicitly: `id` / `label` / `caveats` are shared; outbound has `detected` / `accepts_modes` / `accepts_media` / `max_attachments` / `alt_passthrough` / `char_limit` / `requires_auth`; inbound has `host_patterns` / `ambiguity` / `mode` / `mode_options` / `mode_default` / `extractor` / `recipe` / `mapping` / `h_entry_property` / `auth_required` / `tags_default`. Composer code can iterate either family with the same vocabulary.
2. **Extractors are dispatch types, not fetchers.** `Outpost_Source_Extractor_Base` declares `id()` / `expected_content_types()` / `compute_fetch_url()` / `parse()`. The B2 preview endpoint owns `wp_safe_remote_get` calls; extractors only declare WHICH URL B2 should fetch (recipe-driven for oEmbed, source-URL-verbatim for og_tags / mf2 / rss) and HOW to parse the response. No extractor calls a fetch primitive directly. Documented in `class-extractor-base.php` class docblock.
3. **URL-pattern matcher is bespoke string-comparison code, not regex.** Three accepted shapes: exact host (`open.spotify.com`), suffix wildcard (`*.example.social` — subdomains only, NOT the apex), exact host + path prefix (`boardgamegeek.com/boardgame/`). Plus `*` as the match-any sentinel for `Source_Unknown`. Validation runs at registration time via `Outpost_Source_Base::validate_pattern`, which rejects empty patterns, scheme prefixes, fragments, query strings, regex metacharacters (`[ ] ( ) ? + \ ^ $ | { }`), middle wildcards (`foo.*.example.com`), trailing wildcards (`example.*`), double wildcards, and path patterns missing the trailing slash. **DoS resistance** is the deciding factor: regex matchers paired with attacker-shared URLs can pin a CPU through pathological backtracking; suffix + exact + path-prefix has none of that surface.
4. **`Outpost_Source_Unknown` is the always-last fallback.** Registered lazily via `ensure_bootstrapped()` after the `outpost_sources_init` action fires (so concrete sources register first, then the registry appends Unknown). Capabilities: `host_patterns => ['*']`, `ambiguity => 'ambiguous'`, `mode_options => ['reply','like','repost','bookmark']`, `mode_default => 'bookmark'`, `extractor => 'og_tags'`, `mapping` keyed off `og:title` / `og:description` / `og:image`. Caveats array carries one i18n'd string explaining that no specific adapter detected the URL.
5. **`outpost_source_capabilities` filter is the public override point**, parallel to F2's `outpost_companion_capabilities`. Each `Source_*::capabilities()` applies the filter before returning so site owners can replace any key, restrict mode options, or extend caveats. Filter signature: `(?array $caps, string $source_id) => ?array`. Adapters fall back to their own constructed shape when the filter returns a non-array (defensive against buggy callbacks). Same pattern verified in F2 ActivityPub.
6. **Source_Unknown does NOT work end-to-end until F16.** Its declared extractor is `og_tags`, whose `parse()` is stubbed to throw `Outpost_Source_Extractor_Not_Implemented_Exception`. F5's preview endpoint mitigates by treating `Source_Unknown` matches identically to "no source matched" — falls through to the legacy `wp_safe_remote_get` + script-strip path that returns `{ html, finalUrl, contentType }`. Reply mode keeps working via the legacy path. F6's dispatcher must tolerate the throw equivalently. F16 makes Source_Unknown's extractor functional and the legacy fallback can retire then. **Acceptance criterion 7's "assert the throw" lands in the integration test stub** — the unit-level path returns the legacy shape rather than the exception, by design.
7. **B2 endpoint integration is additive, not replacing.** `Outpost_Preview_Endpoint::handle_request` gained an optional `source_id` parameter and a new dispatch branch. Three paths: (a) explicit `source_id` resolves to a registered non-Unknown source → extract via that source's recipe + extractor + mapping, return new `{ source_url, source_id, extracted, raw, warnings }` shape. (b) No `source_id` but `find_for_url` returns a non-Unknown source → same. (c) Source_Unknown matches OR no source registered → legacy `{ html, finalUrl, contentType }` path. SSRF defenses (wp_safe_remote_get's host-is-external filter chain), size cap, content-type validation, rate limit run BEFORE extractor parse in BOTH paths — the only thing extractor dispatch changes is which URL gets fetched (oEmbed templates the source URL into a recipe-declared endpoint) and which content-type prefixes are allowed (extractor-supplied vs the legacy text/html allowlist).
8. **`Outpost_Source_Extractor_Oembed` is the only concrete extractor in F5.** Six stubs (og_tags, mf2, rss, api_json, api_xml, composite) throw `Outpost_Source_Extractor_Not_Implemented_Exception`. The Oembed implementation: `compute_fetch_url` substitutes the source URL (rawurlencoded) into the recipe's `endpoint` template's `{url}` placeholder; refuses recipes without the placeholder (visible InvalidArgumentException) so silent endpoint reuse never happens. `parse` JSON-decodes; rejects oversized bodies (1 MB cap, defense-in-depth above the endpoint's 5 MB cap), rejects non-array decoded shapes, rejects JSON-list shapes (oEmbed responses are objects, not lists). Empty objects are accepted (the source's mapping handles missing fields).
9. **Naming divergences from prompt.** Repo conventions take precedence: `Source_Base` → `Outpost_Source_Base` (file `class-source-base.php`); `Source_Loader` → `Outpost_Source_Registry` (mirrors `Outpost_Companion_Registry`); `Source_Unknown` → `Outpost_Source_Unknown`. Tests in `tests/unit/` not `tests/sources/`. Exception class lives at `includes/sources/exceptions/class-extractor-not-implemented-exception.php`. Each divergence captured in this list so future Phase F prompts know what to expect.
10. **`do_action('outpost_sources_init')` fires from `ensure_bootstrapped()`.** Concrete sources subscribe to that action and register themselves; the registry then appends `Source_Unknown` so it's always last. Unit tests register sources directly (WP_Mock doesn't dispatch listeners for arbitrary actions); integration tests will exercise the production action-based wiring once wp-env lands.
11. **Tests added: 67.** PHPUnit count 182 → 249. Coverage:
    - SourceBaseTest (35 tests): pattern validation across every accepted + rejected shape, `pattern_matches` for exact / suffix / path / case-sensitivity, default `matches_url` / `recipe_for_url` / `mode_for_url` behaviors, `map_extracted` with simple field mapping + `@source_url` + `@now` substitutions + null-target drop + missing-key drop.
    - SourceRegistryTest (12 tests): Source_Unknown auto-registration, find_for_url fallback, find_for_url precedence with concrete sources, get_by_id, duplicate-id rejection, malformed-pattern rejection, empty-id rejection, `get_extractor` for all seven types + unknown.
    - SourceUnknownTest (6 tests): capabilities shape, matches-any URL, bookmark default mode, filter-can-modify, filter falls back on non-array, mapping handles og:* keys.
    - ExtractorOembedTest (10 tests): id, content types, URL-token substitution, recipe-shape rejection (missing endpoint, missing placeholder, non-string endpoint), parse canonical response, rejects non-JSON, rejects array shape, rejects oversized body, accepts empty object.
    - PreviewSourceDispatchTest (4 integration stubs): legacy path preserved without source_id, explicit source_id=unknown returns 501, SSRF blocked when recipe points at localhost, oEmbed dispatch returns extracted shape.
12. **Concrete sources land in F7+; F5 ships only the foundation.** Empty-by-design — the architecture lives, no concrete source registers. F6 wires the `/post/share-target` endpoint to `Source_Registry::find_for_url` and surfaces the mode-picker UI for ambiguous sources. F7 ships Source_Spotify (oEmbed, unambiguous → listen mode). F15 ships Source_YouTube (also oEmbed). F16 ships Source_Unknown end-to-end via the og_tags parser.
13. **§5 audit lint passes against this commit.** All 5 sub-checks (B1–B5) clean. The case-study handle list, instance allowlist, and fixture-handle allowlist needed no extension — F5's test fixture (`tests/fixtures/source-test-fakes.php`) uses `example.com` and `example.test` exclusively. Source_Unknown's `*` host pattern is allowlisted as the F5 sentinel via `Outpost_Source_Base::validate_pattern`'s explicit early-return; it doesn't reach the B3 fediverse-host regex.
14. **Coverage on `includes/sources/*` ≥85% (manual gap analysis).** Coverage tooling (Xdebug / pcov) lands in a future Phase G session; manual analysis of the new code: every branch in `validate_pattern`, every path in `pattern_matches`, every default-method body in `Source_Base`, every branch in `Source_Registry::register` / `find_for_url` / `get_by_id` / `get_extractor`, every branch in `Extractor_Oembed::compute_fetch_url` and `parse`, and the filter-passthrough + filter-replace + filter-non-array paths in `Source_Unknown::capabilities` are exercised by the 67 new tests.
15. **Follow-ups for later sessions:**
    - F6: dispatcher at `/post/share-target`. Reads URL from share intent, calls `find_for_url`, redirects to composer with mode + extracted-fields query params.
    - F7: Source_Spotify (concrete, oEmbed, unambiguous → listen).
    - F15: Source_YouTube (oEmbed, unambiguous → watch). Reuses Extractor_Oembed unchanged.
    - F16: Extractor_Og_Tags concrete implementation. Source_Unknown becomes end-to-end-functional; Reply mode can switch from the legacy path to the structured-extraction path. CLAUDE.md F5 #6's mitigation lifts.
    - Future: Extractor_Mf2 / Rss / Api_Json / Api_Xml / Composite implementations as their consumer sessions land.

## Session F6 — Locked Decisions (shipped)

Closed 2026-05-04. Share-target dispatcher. Server-side decides what composer route a shared URL maps to (auto vs picker), 303-redirects to the composer with the appropriate query params. F6 ships against Source_Unknown only (the F5 default) — the dispatcher mechanism works without any concrete source registered. F7 ships Source_Spotify and verifies the dispatcher is correct end-to-end.

1. **Dispatch is stateless; redirect-based handoff.** `Outpost_Source_Detector::dispatch( $url, $context )` is a pure function over the URL + currently-registered sources. Returns a route-decision array `{ route_type, mode|mode_options, source_id, redirect_url, prefill_token? }`. The HTTP 303 redirect is the entire handoff between server-side dispatch and client-side composer rendering — no session storage, no transient between requests. Composer reads its query params; that's it.
2. **URL extraction priority chain.** Different platforms put the URL in different fields. The chain: (1) `url` field if it's a valid http(s) URL, (2) `text` field starting with http(s)://, (3) `text` field containing an http(s) URL anywhere (regex-extracted, validated via wp_parse_url), (4) `title` field starting with http(s)://. Returns null when no URL is detected — caller routes to the share-text-only flow (Note mode with title/text pre-filled, no source URL). Implemented in `Outpost_Source_Detector::extract_url_from_payload`.
3. **iOS Shortcut bridge endpoint at `/post/shortcut` accepts JSON.** Web Share Target API never landed in iOS Safari (WebKit bug 194593, still open as of May 2026). The iOS Shortcut bridge POSTs `{ url, shared_text? }` to this alias endpoint. Same dispatcher under the hood; just JSON in / 303 redirect out. The Shortcut .plist generation is a separate session (Phase E or later) — F6 ships only the receiving endpoint so the Shortcut author has a fixed contract.
4. **Pre-fill preview is a fire-and-forget transient.** For unambiguous (auto) routes, `Outpost_Share_Target_Controller::enqueue_preview_transient` writes a per-user, per-URL-hash transient marker before the redirect issues. The composer reads the cache slot via the existing `/preview` endpoint on mount; cache miss falls back to a fresh preview fetch. The transient is an optimization, not a dependency. TTL 5 minutes (long enough for the redirect roundtrip; short enough not to bloat the DB). Ambiguous (picker) routes do NOT enqueue — the user picks mode first, since different modes may want different recipes (Reply wants citation context; Bookmark only wants the URL).
5. **Source_Unknown is the universal fallback; F6 tests it as the only-registered-source baseline.** Every URL dispatches to mode-picker with Bookmark default when only Source_Unknown is registered. This is the F5 default state — F6 verifies the dispatcher works end-to-end without depending on any concrete source.
6. **No platform branching in dispatch logic.** `$context['platform']` exists for telemetry only — never branches dispatch. Behavior is identical regardless of whether the request came from the Android share sheet, the iOS Shortcut bridge, or a desktop bookmarklet. Keeps dispatch deterministic and reproducible. Documented in `Outpost_Source_Detector::dispatch` via an explicit `unset( $context )` after the function signature.
7. **Bespoke URL-pattern matcher inherits F5's "registration order is priority" rule.** F6's "exact host beats wildcard" test confirms the contract: source authors register exact-match patterns BEFORE wildcards, and the first match wins. Documented explicitly in the F6 test docblock so future Phase F sessions don't expect different ordering semantics.
8. **Layered SSRF defense.** Dispatch is pure URL → redirect, doesn't fetch. SSRF guards live at the F5 preview endpoint via `wp_safe_remote_get`'s `http_request_host_is_external` filter chain. F6 dispatch can build a redirect URL pointing the composer at a localhost URL; the eventual B2 fetch rejects per F5's defenses. Test verifies both layers separately (dispatch builds redirect for localhost; B2 blocks fetch). Composer cannot bypass either layer.
9. **`Outpost_PWA_Shell::halt()` is now public** so F6's controllers can halt the request after issuing redirects without duplicating the testing-define seam. Same exit semantics as before — `OUTPOST_TESTING_PWA_SHELL` define skips the actual `exit` so unit tests work.
10. **PWA-side glue is minimal: `parse_dispatch_params` extends `share-target.ts`.** Reads F6-specific query params (`mode` / `picker` / `default` / `source` / `cached_for` / `url` / `text` / `title`) and emits the existing `ShareTargetData` shape so the composer's existing mode-mount logic (`ComposerTabs` + per-mode `consume_share_target_for_*`) picks up the data without changes. Composer entry in `index.tsx` calls the new parser on `/post/` route mount, stashes if non-null, and strips query params via `history.replaceState` so refresh doesn't re-stash. Bundle: +0.17 KB gzipped (33.52 → 33.69, 84% of Phase C budget).
11. **Naming divergences from prompt.** Repo conventions take precedence: `Source_Detector.php` → `includes/sources/class-source-detector.php` with class `Outpost_Source_Detector`; `Share_Target_Controller.php` → `class-share-target-controller.php` with class `Outpost_Share_Target_Controller`; `Shortcut_Controller.php` → `class-shortcut-controller.php` with class `Outpost_Shortcut_Controller`. Tests in `tests/unit/` not `tests/sources/`.
12. **Tests added: 29.** PHPUnit count 249 → 278. New tests: SourceDetectorTest (19) covers URL extraction priority across all 5 cases (url field / text-as-URL / text-contains / title / no-URL), dispatch with Source_Unknown only, dispatch with concrete unambiguous source, exact-host-beats-wildcard via registration order, prefill_token determinism, per-request memoization, layered SSRF defense. Two integration test stubs (5 + 5) document the wp-env-pending end-to-end paths: ShareTargetDispatchTest (Web Share Target POST flows) and ShortcutDispatchTest (iOS Shortcut JSON flows).
13. **`OUTPOST_VERSION` bumped 0.1.68 → 0.1.69.** New rewrite rule (`/post/shortcut`) means the rewrite cache must flush per A2 #11/#16. The version-bump-as-flush-trigger mechanism kicks in automatically on update; no admin action required.
14. **§5 audit lint passes.** All 5 sub-checks (B1-B5) clean. F6 test fixtures use `example.com` exclusively — no case-study handles. The new `/post/shortcut` route is generic; no platform-specific names.

## Session F7 — Locked Decisions (shipped)

Closed 2026-05-04. First concrete inbound source — Spotify. Validates F5+F6 are correct end-to-end with a real adapter.

1. **Source_Spotify validates F5's design with capabilities()-only implementation.** Adapter overrides nothing. Source_Base's defaults handle `matches_url` (host_patterns), `recipe_for_url` (verbatim recipe), `mode_for_url` (the unambiguous mode), and `map_extracted` (mapping with `@source_url` substitution). A reflection-based test asserts the only declared method on `Outpost_Source_Spotify` is `capabilities` — if a future change adds an override, the test fails loudly so the override gets justified in CLAUDE.md as a Source_Base design gap rather than smuggled through.
2. **oEmbed is the canonical extraction path; Web API is not used.** Spotify's Web API requires OAuth + (since Feb 2026) a Premium subscription. oEmbed at `https://open.spotify.com/oembed?url={url}` is anonymous, no API key, no advertised rate limit. §5-clean. Recipe declares `endpoint` template + `response_format: json`; F5's Extractor_Oembed handles substitution + parsing.
3. **Spotify URL variants handled.** `host_patterns` declares `open.spotify.com` (exact host) and `spotify.link` (exact host for short links). The host-only pattern claims every path: track, album, episode, show, playlist, intl-{lang} regional variants. Tests verify all 9 variant forms match. spotify.link short links rely on oEmbed following the redirect — verified in F7 live device test (acceptance #9).
4. **`/artist/` URLs match Spotify today, not Source_Unknown.** F5/F6's host-only patterns can't distinguish path within open.spotify.com. The user lands in Listen mode with empty p-name; can switch modes via the C0 mode-switcher. Future UX research may justify a `recipe_for_url` override that returns a different mode for `/artist/` paths — F5's template-method seam supports this without further design changes. Documented in adapter docblock + integration test stub (`artist_url_also_matches_spotify_pattern_today`).
5. **Mapping per concepts/capture-inbound-may-2026.md §3.1.** title→p-name, thumbnail_url→u-photo, provider_name→p-publication, @source_url→u-listen-of (always set, regardless of oEmbed success). The @source_url macro inside the mapping array is documented self-evidently; future readers see u-listen-of as part of the mapping shape rather than buried in the source's `h_entry_property` declaration.
6. **Degraded extraction paths produce a working composer.** D1 (404 / track deleted) → preview endpoint surfaces fetch_failed 502; composer falls back to manual entry. D2 (503 / Spotify down) → same. D3 (malformed JSON) → Extractor_Oembed::parse throws RuntimeException, preview surfaces 502. D4 (timeout) → wp_safe_remote_get returns WP_Error, preview surfaces 502. In every case u-listen-of is set client-side from the URL the user shared; only auto-fill of title / cover / publication is degraded. Tests cover D1 and D3 directly with offline fixtures; D2 and D4 share D1's code path (HTTP error handling) so don't need separate fixtures.
7. **Test fixtures use synthetic placeholder values, not a real track.** The fixture URL pattern (`/track/0000000000000000000000` — 22 zeros) is syntactically valid but doesn't resolve; tests mock the response body. Adapter code references zero specific tracks. §5 audit lint clean.
8. **Registration via `outpost_sources_init` action.** The hook fires from inside `Outpost_Source_Registry::ensure_bootstrapped` BEFORE the registry appends Source_Unknown, guaranteeing Spotify (concrete, exact-host) wins over Unknown (`*` fallback). Tests verify the order via direct registration + find_for_url assertions; the action firing is exercised in production boot, integration tests cover the wp-env-pending end-to-end run.
9. **Naming divergences from prompt.** `includes/sources/in/Source_Spotify.php` → `includes/sources/class-source-spotify.php` with class `Outpost_Source_Spotify` (continues F5/F6 convention; no `in/` subdir). Tests in `tests/unit/` not `tests/sources/`.
10. **Tests added: 35.** PHPUnit count 278 → 313. SourceSpotifyTest covers 26 paths: capabilities shape (3), URL pattern matching for 9 variants, 3 negative-pattern cases (non-Spotify URLs, Apple Music, accounts.spotify.com subdomain — exact-host doesn't match), mode_for_url across variants, mapping for track + album + episode fixtures, degraded D1 (404 body parses but mapping drops absent keys) and D3 (malformed JSON throws), oEmbed compute_fetch_url substitution, registry integration (find_for_url returns Spotify for Spotify URLs, Source_Unknown for others), filter override. Plus the reflection test asserting capabilities()-only. Integration test stub `SpotifyEndToEndTest` documents 9 wp-env-pending paths covering the full pipeline.
11. **§5 audit lint passes.** All 5 sub-checks clean. Adapter references no specific track / album / episode / playlist / handle.
12. **Live device verification deferred to one-time check.** Per acceptance #9, manually verify on Android that sharing a Spotify track to Outpost auto-routes to Listen mode with details pre-filled. NOT a CI requirement; documented in the F7 PR as the validation that share-sheet → composer-pre-fill works for real users.

## Session F8 — Locked Decisions (shipped)

Closed 2026-05-04. Two-part deliverable: hardened Source_Spotify coverage (Part A) and the offline fixture pattern formalized for every later `Source_*` (Part B). Pattern lives at `tests/fixtures/sources/{source_id}/{scenario}.{ext}`; F15+ inherits.

1. **Fixture directory convention is locked: `tests/fixtures/sources/{source_id}/{scenario}.{ext}`.** Every Phase F `Source_*` adapter ships fixtures under its own subdirectory keyed off the source's id (`spotify`, future `youtube`, `vimeo`, etc.). Each subdirectory has a `README.md` populated from `tests/fixtures/sources/_TEMPLATE_README.md` (capture date, source URL, sanitization, last-verified-live date). F7's five fixtures migrated from `tests/fixtures/spotify-*.json` to `tests/fixtures/sources/spotify/oembed-*.json` and renamed (no `spotify-` prefix — it's redundant inside the source's directory).
2. **`SourceFixtureLoader` and `MockHttpClient` live under `Outpost\Tests\Helpers\`.** New autoload-dev PSR-4 mapping `tests/helpers/` → `Outpost\Tests\Helpers\`. Loader exposes `load_oembed_fixture()`, `load_html_fixture()`, `load_json_fixture()`, and `load_raw_fixture()` (raw is for tests that want unparsed bodies — malformed JSON, non-JSON 503 fixtures). Missing fixtures throw with the path in the message so the failure names what's missing rather than producing a confusing "json_decode returned null" downstream.
3. **MockHttpClient is a test-layer fixture-driven response builder, not a production injection seam.** F8's prompt asked for `Extractor_Oembed::set_http_client()`, but F5's design has Extractor_Oembed only doing `compute_fetch_url()` and `parse()` — the HTTP fetch lives in `Outpost_Preview_Endpoint::handle_request`. Adding a setter to Extractor_Oembed would be dead code. Net: MockHttpClient produces `wp_safe_remote_get`-shaped response arrays; integration tests that exercise the preview-endpoint pipeline (currently wp-env-pending) wire it via `WP_Mock::userFunction('wp_safe_remote_get')->andReturnUsing(...)`. Unit tests for `Extractor_Oembed::parse()` and `Source_Spotify::map_extracted()` work directly off `SourceFixtureLoader::load_oembed_fixture()` — there's no HTTP layer to mock.
4. **Live tests use `@group live` class annotation + `WP_TESTS_LIVE=1` setUp guard (defense-in-depth).** Two layers of skip: phpunit.xml.dist's `<groups><exclude>live</exclude></groups>` excludes the group from the default suite (tests don't even load), AND `setUp()` checks the env var and `markTestSkipped()`s if missing. Either layer alone would protect CI; both means a maintainer can opt into running live tests via `--group=live` alone (rare) without leaking them into the default. `composer test:live` runs `WP_TESTS_LIVE=1 phpunit --group=live` — sets both gates green at once.
5. **Live test annotation MUST attach directly to the class, not to the file-level docblock.** PHPUnit 9.6 doesn't pick up class-level `@group` from the first docblock in a file (which it treats as the file docblock). The annotation needs its own docblock immediately above `final class SourceSpotifyLiveTest extends TestCase`. Captured here so future Source_*LiveTest files follow the working pattern from the start.
6. **Live tests use PHP-native `file_get_contents` + stream context, NOT `wp_safe_remote_get`.** The unit-test bootstrap doesn't load WordPress core, so wp_safe_remote_get is unavailable. Live tests verify the third-party API contract (response shape); the preview-endpoint integration (SSRF defenses, content-type validation, size cap, rate limit) is covered by wp-env integration tests. The two layers test different things: API contract vs request-handling.
7. **Live tests skip on network failure, do not fail.** `file_get_contents()` returns false on connection failure; the test calls `markTestSkipped()` rather than `fail()`. Reasoning: a developer on an airplane shouldn't have `composer test:live` fail when their network is down — it should report "couldn't reach Spotify, try again" not "Spotify is broken." Real contract drift produces a different failure (assertion mismatch on the parsed response), distinguishable from network unavailability.
8. **Default live target URL is configurable via `OUTPOST_TEST_<SOURCE>_URL` env var.** `SourceSpotifyLiveTest` defaults to a durable Daft Punk track URL (on Spotify since launch, widely distributed); the env override is the maintenance path when that resource ever moves. Future `Source_*LiveTest` files follow the same env-var convention.
9. **§5 audit lint already covers `tests/fixtures/sources/` recursively.** B1's path scope is `tests/`; B5's path scope is `tests/fixtures/`. Both are `grep -r` scopes, so the new subdirectory is scanned without lint changes. F8 commit doesn't extend the lint; documenting here so a future Phase F session knows the lint reach is "everything under tests/" — adding fixtures elsewhere would require extending the lint.
10. **Spotify fixture set: 17 files.** 5 happy paths (track, album, episode, show, playlist) + 12 edge cases (no-thumbnail, html-entities, utf8-korean, utf8-arabic, utf8-cyrillic, script-injection, javascript-url, empty-title, very-long-title, 404 error body, 503 non-JSON HTML, malformed JSON). UTF-8 fixtures use public-domain folk songs (아리랑 Korean, موسيقى الكلاسيكية Arabic generic phrase, Калинка Russian) — real non-ASCII content rather than `lorem ipsum`, which proves UTF-8 actually flows through, but not personal data. Synthetic 22-zero placeholder track ID convention from F7 preserved across all fixtures.
11. **Adapter contract reaffirmed: data-shape declarative, NOT sanitizing/decoding/truncating.** Part A's defensive tests (script tags, javascript: URLs, file:// URLs, zero-width unicode in provider name) all assert the adapter passes the raw value through. Sanitization lives at the layer that needs it: preview endpoint strips scripts in HTML responses; composer pre-fill assigns to `<input value>` via DOM (browser handles entity decoding); render-time `esc_url()` rejects non-http(s) schemes. Doing it in the adapter would silently transform user-visible data and double-encode anything Spotify already decoded. The transparent-adapter contract is now test-enforced.
12. **Tests added: 28.** PHPUnit count 313 → 339 (excluding 2 live tests skipped from default suite). 26 new in `SourceSpotifyExpandedTest`: 8 A1 (extra fields, html entities, korean, arabic, cyrillic, very-long, scdn, no-thumbnail) + 4 A2 (script, javascript-url, file-url, zero-width unicode) + 6 A3 (si tracking, fragment, intl+query, no-trailing, trailing-slash, fetch_url query preservation) + 8 A4 (all-missing, null-treated-as-missing, empty-string, show, playlist, 503, null-provider, listen-of-always). 2 new in `SourceSpotifyLiveTest` (oembed contract, mapping h-entry shape). F7's 26 unit tests refactored to use `SourceFixtureLoader` — assertion count preserved.
13. **No version bump.** F8 ships test infrastructure + fixtures; no production-code change ships to users. CLAUDE.md's A2 #16 rule (any deployable behavior change moves the version) doesn't trigger.

## Session F9 — Locked Decisions (shipped)

Closed 2026-05-04. Foundation of the manual cross-post adapter — the umbrella companion that registers manual-share targets for §5-impossible silos (Instagram, Facebook, X, LinkedIn, Threads, TikTok, Pinterest, Reddit, Flickr). F9 ships the declarative layer only; F10 lands Android intent firing, F11 iOS, F12 silo URL capture, F13 audit log surfacing.

1. **One umbrella companion, ten platforms.** `Outpost_Manual_Share_Adapter` extends `Outpost_Companion_Base` and contributes ten chips via a NEW method `platform_chips()`. Per-platform configs are declarative arrays (no per-platform PHP class) — quirks like `caption_via`, `ios_strategy`, `android_pkg`, `EXTRA_TEXT` honoring all live in config fields the F10/F11 intent-fire logic reads at runtime.
2. **Always-active without modifying the detector.** The umbrella's `file()` returns `OUTPOST_PLUGIN_BASENAME` so `Outpost_Companion_Detector::status()` reports `'active'` whenever Outpost itself is loaded. No detector changes needed; `is_active()` stays final on Companion_Base.
3. **`capabilities()` returns null; `platform_chips()` is the chip surface.** The umbrella itself is NOT a syndicate-to chip — returning null prevents `Outpost_Micropub_Bridges::merge_syndicate_chips()` from generating a meta-chip. Chips come from `platform_chips()` which the registry's `chips_for_mode()` enumerates alongside the F2 `capabilities()` chip via a new `collect_chips_from_adapter()` helper. `method_exists()` gates the platform_chips() call so the new contract is optional — feature-surface adapters keep working unchanged.
4. **`Outpost_Manual_Share_Platform_Config` validates strictly at construction.** Required keys (`id`, `label`, `icon`, `accepts_modes`, `caption_via`, `after_share`) MUST be present with correct types. Enum-typed fields (caption_via, after_share) reject unrecognized values. `id` MUST be kebab-case. Malformed configs throw `Outpost_Manual_Share_Invalid_Config_Exception` at registration time — a typo in a filter-registered platform surfaces during boot, NOT later when a user taps a chip.
5. **`prefers_bridgy` flag is strict-true only.** Only literal `true` enables Bridgy-defer for a platform. String "true", integer 1, etc. produce false. Defends against accidental enabling from misencoded filter values.
6. **`outpost_manual_share_platforms` filter is the documented extension point.** Site owners append/modify/remove platforms through this filter. Adding VSCO, Glass.photo, 500px, etc. is a one-call site-config change, no Outpost fork needed. CONTRIBUTING.md documents the shape with a Glass example. Tests demonstrate append, remove, and modify paths.
7. **Reddit-manual and Flickr-manual ship with `prefers_bridgy => true`.** A separate `outpost_manual_share_defer_to_bridgy` filter signals whether Bridgy Publish is configured; F14 will replace the default-false implementation with real `Companion_BridgyPublish::is_configured()` detection. Until F14, both chips remain visible because the defer filter returns false. The hide-when-Bridgy logic lives in `Outpost_Manual_Share_Adapter::platform_chips()` and is tested by mocking the defer filter to true.
8. **F9 ships photo/gallery chips only — text-only manual share is deferred.** The 10 platforms declare `accepts_modes => ['photo', 'gallery']` (TikTok adds video). X/Threads/Pinterest could in principle accept text-only Notes, but F9 scope is photo/gallery to validate the structure. Text-only manual share is a follow-up session; documented as deferred.
9. **REST endpoints under `/wp-json/outpost/v1/manual-share-*`.** `Outpost_Manual_Share_Controller` registers two routes:
   - `POST /manual-share/intent` — F9 stub returns `{status: stub, message, platform_id, post_id}` so the composer renders "intent firing not yet implemented (F10 Android / F11 iOS)" without requiring real intent logic.
   - `GET /manual-share-chips?mode=...` — Server-filters `chips_for_mode()` to entries carrying the `manual_share` extension key, so AP chips drop out and the composer's manual-share strip gets only manual platforms.
   Both routes share the syndicate-targets endpoint's auth posture: `show_in_index => false`, cookie/edit_posts/bearer permission, no SSRF surface. F9 prompt's "?q=manual-share-chips" Micropub query path didn't fit Outpost's "Shanske owns Micropub server semantics" architecture (per F1 #3) — REST mirrors syndicate-targets pattern instead.
10. **Naming divergences from prompt.** Prompt: `Companion_ManualShare.php`, `Platform_Config.php`, `Platform_Registry.php`, `Manual_Share_Controller.php`, `tests/companions/`. Actual repo conventions: `class-manual-share-adapter.php` with class `Outpost_Manual_Share_Adapter` (matches the `Outpost_*_Adapter` regex enforced by `Outpost_Companion_Registry::resolve_classes`); `includes/companions/manual-share/` subdir uses kebab-case; tests in `tests/unit/` with the existing pattern. Critical: the registry's class-name regex blocks adapters from outside the Outpost namespace — divergence from prompt's `Companion_ManualShare` was non-negotiable.
11. **WP_Mock `onFilter()->reply(false)` does NOT make `apply_filters` return false.** Discovered during F9 test development. Workaround: omit the filter mock entirely and rely on WP_Mock's default behavior (`apply_filters('hook', $default)` returns `$default` when no filter is registered). For tests that need `false`, do not mock the filter; only mock when you need a non-default reply (e.g. `true`). Captured here so F10+ tests against `outpost_manual_share_defer_to_bridgy` and similar boolean filters use the working pattern.
12. **Tests added: 33.** PHPUnit count 339 → 397 (with 9 new wp-env-pending integration stubs in `ManualShareIntegrationTest`). 16 new in `PlatformConfigTest` (required keys, enum validation, kebab-case id, defaults, prefers_bridgy strict-true, caveats/accepts_media filtering, full round-trip). 11 new in `PlatformRegistryTest` (10 default platforms, append/remove/modify via filter, malformed-throws, cache, Reddit+Flickr prefers_bridgy, all platforms accept photo or gallery). 12 new in `ManualShareAdapterTest` (file/label/feature_slugs/capabilities, platform_chips count, Bridgy-defer hides Reddit+Flickr, Companion_Registry::chips_for_mode integration, fail-open on unknown mode). 10 new in `ManualShareControllerTest` (intent stub success, invalid post_id/platform_id rejection, unknown platform error data, whitespace trimming, chips endpoint per-mode, listen mode empty, fail-open, no-mode all-chips). Existing CompanionRegistryTest updated: default-adapters count 7 → 8, chips_for_mode null count 1 → 11 (1 AP + 10 manual-share platforms).
13. **No version bump.** F9 ships test infrastructure + REST surface + new companion declared but no observable user-facing behavior — chip rendering wires up in F10/F11 client-side. CLAUDE.md A2 #16 rule defers to F10's deployable behavior change.

## Session F10 — Locked Decisions (shipped)

Closed 2026-05-04. Replaces F9's stub for the Android branch with real intent firing: pre-stage media, write clipboard, fire `navigator.share()` with files, fall back to intent:// URL when needed. iOS still routes to the F9 stub until F11. Audit-log scaffold writes entries on chip-tap (F12 will extend with completion-tracking; F13 surfaces).

1. **Platform config drives behavior; no platform-name branches in code.** `Outpost_Manual_Share_Intent_Payload_Builder` reads `Platform_Config::to_array()` for `android_action`, `android_pkg`, `android_mime`, `android_extras`, `web_intent_url`, `caption_via`, `after_share`. Quirks like Facebook ignoring `EXTRA_TEXT` or TikTok using `com.zhiliaoapp.musically` are config values, not if-statements. F9's platform configs were deliberately rich enough that F10 needed zero per-platform code paths.
2. **navigator.share preferred; intent:// URL fallback; two-tap worst case.** PWA-side `execute_android_share` is a deterministic fallback chain — clipboard always writes first, then native share with files, then `window.location.href = fallback_url`, then a structured `'aborted'` two-tap result if no path is viable. Telemetry posts on every outcome.
3. **Web intent URL is iOS-only on F10's reading; Android falls through to intent://.** When a platform declares `caption_via='intent'` (X, LinkedIn, Pinterest) AND `web_intent_url` is set, F10's `build_fallback_url()` deliberately ignores the web intent on Android — the intent:// URL is the better path because Android honors EXTRA_TEXT and the web intent doesn't accept image attachment. F11 will route iOS to the web intent URL where it's the only viable path. Captured here so F11's iOS branch knows to consult `caption_via` + `web_intent_url` together.
4. **`caption_via='web_intent'` is the explicit primary-web-intent signal.** Threads, Reddit-manual, Flickr-manual declare this; their strategy is `intent_url` (PWA navigates straight to the web URL, skipping `navigator.share`). Pinterest/X declare `caption_via='intent'` because Android EXTRA_TEXT works — strategy stays `navigator_share` with intent:// as fallback. The two cases ARE different; F9's configs encode the distinction.
5. **EXTRA_STREAM/EXTRA_TEXT short names expand to canonical Android extras.** F9 platform configs use short names (`EXTRA_STREAM`, `EXTRA_TEXT`); the intent:// URL builder maps them to `android.intent.extra.STREAM` / `android.intent.extra.TEXT` because Android target apps look up by the canonical name. Unknown short names pass through verbatim so future platform-specific extras (Instagram Stories' `interactive_asset_uri`) work without map updates.
6. **Audit log writes on intent fire with outcome='unknown'; PWA telemetry updates with real outcome.** `Outpost_Manual_Share_Audit_Log::add_entry()` runs from the payload builder during the POST `/manual-share/intent` request and returns the entry id in the response. The PWA fires `POST /manual-share/intent/log` with `{post_id, audit_log_id, outcome}` after the share resolves; the controller's `handle_log_request` patches `outcome` via `update_entry`. Net effect: even if the PWA crashes before telemetry, the audit log records the firing attempt.
7. **`outpost_manual_share_log` post-meta is the single storage location.** Per-post layout means a single post's share history is one query; cross-post analytics is a future concern (Phase H+). Auto-cleans on post deletion, integrates with WP backup/export tooling.
8. **Per-post `current_user_can('edit_post', $post_id)` check inside `handle_request`.** Acceptance criterion #10: cross-user post_id rejected with 403. Bearer-token auth via the IndieAuth plugin sets the current user before this check; the per-post cap then applies. The endpoint's `permission_callback` still runs first (cookie/edit_posts/bearer presence — coarse-grained); the per-post check is the second layer.
9. **WP_Post stub added to tests/bootstrap.php.** F10's `Intent_Payload_Builder` does `instanceof WP_Post` checks for type safety. The unit-test bootstrap previously only stubbed `WP_Error`, `WP_REST_Request`, `WP_REST_Response`, `WP_Term`. F10 added `WP_Post` to the same pattern so future tests touching post objects work without per-test stub gymnastics.
10. **WP_Mock function-mock re-registration is brittle; use class-property dispatch.** Discovered during F10 test development. Re-registering `WP_Mock::userFunction('foo')` mid-test produces unpredictable behavior (the new registration may layer over rather than replace the setUp default). Reliable pattern: setUp registers a closure that consults a class property (e.g. `$this->meta_store`, `$this->user_can_edit_post`); individual tests flip the property to change behavior. Captured for F11+ tests against `current_user_can` / `get_post` / similar.
11. **`@image_uri` is a runtime placeholder filled by the PWA.** The server's intent:// URL contains `%40image_uri` (the URL-encoded form because it lives in a query parameter). The PWA's `fill_intent_url()` substitutes BOTH the raw `@image_uri` token and the percent-encoded `%40image_uri` form. Practical reality: `@image_uri` substitutes to a `blob:` URL (the only file-URL the PWA can construct), which Android target apps generally don't accept in `EXTRA_STREAM`. The intent:// URL fallback is therefore most useful for caption-only platforms; image-bearing intents rely on `navigator.share()` directly.
12. **PWA tests inject deterministic environments — no real navigator/clipboard/fetch touched.** `AndroidShareEnvironment` carries the four primitives (`navigator_share`, `navigator_can_share`, `clipboard_write_text`, `fetch`) plus `navigate` (window.location.href setter) and `post_telemetry` (the audit-log POST). `default_environment()` builds from globals for production; tests pass synthetic environments. 33 Vitest tests cover happy path + every fallback branch + clipboard resilience + telemetry resilience.
13. **PWA composer chip-strip rendering UI is deferred.** F10 ships server payload + PWA modules; the actual composer-side "tap chip → execute_android_share" wiring is a separate concern that lands when both Android (F10) AND iOS (F11) handlers exist, so the strip can route by platform without re-rendering. F11 will land iOS handler + the unified composer integration. Documented as F10's deferred follow-up rather than a missed acceptance criterion.
14. **Tests added: 34 PHP + 33 PWA = 67.** PHP 397 → 431 (`AuditLogTest` 12, `IntentPayloadBuilderTest` 14, `ManualShareControllerTest` 10 new + 8 updated F9 tests). PWA 172 → 205 (`platform.test.ts` 9, `intent-url.test.ts` 13, `android.test.ts` 11). All gates green: PHPCS, PHPStan, §5 audit, PHPUnit, Vitest.
15. **No version bump.** F10 ships server-side intent payload generation + audit log + PWA modules. The composer integration (F11+) is the user-observable change; F10 is foundation that F11 wires in.

## Session F11 — Locked Decisions (shipped)

Closed 2026-05-04. Replaces F10's iOS stub with real strategy-chain handler. Per-platform fallback chains drive everything: navigator_share_files → app_url_scheme → web_intent → manual. F11 modules ship; the composer chip-strip integration that wires Android (F10) + iOS (F11) together remains deferred (consistent with F10 #13 stance).

1. **iOS strategy chains are arrays of strategy names; F9's string-typed `ios_strategy` migrated to array.** F9 originally declared `ios_strategy` as a string ('navigator_share_files', 'web_intent', etc.). F11 migrates it to an array — each platform's chain like `['navigator_share_files', 'app_url_scheme', 'manual']` walked in order by the runner. Backward-compatible: a string `ios_strategy` value coerces to a single-element array at config-validation time. Site owners adopting F11 don't have to migrate immediately.
2. **`app_url_scheme` replaced F9's `ios_url`.** F9's `ios_url` field mixed iOS app URL schemes (`instagram://`) and iOS web intent URLs (`https://twitter.com/intent/tweet`). F11 separates these: `app_url_scheme` for the iOS app URL scheme; `web_intent_url` (already F9) for the iOS-friendly web URL. Backward-compat: `ios_url` from F9 silently maps to `app_url_scheme` at validation time.
3. **Per-platform iOS chains per Doc 1 §4.4.** Instagram (feed + Stories), LinkedIn, TikTok, Flickr-manual use `[navigator_share_files, app_url_scheme, manual]`. Facebook, X, Threads, Pinterest, Reddit-manual use `[navigator_share_files, web_intent, manual]`. Every chain starts with `navigator_share_files` (best UX when in PWA) and terminates with `manual` (always-succeeds worst case). Cross-platform invariants are test-enforced — `IosStrategyChainsTest` catches drift.
4. **The strategy chain is platform-routed; the runner is a tiny dispatcher.** `StrategyRunner.run_strategy_chain()` walks the chain, calls each strategy's handler, stops at the first non-`rejected` outcome. `'aborted'` (user cancelled) does NOT fall through — tapping cancel on the share sheet must NOT chain into a URL scheme launch. `'fired'` stops the chain. Defensive default: if the chain exhausts without firing, the runner returns `outcome='aborted', strategy='manual'`.
5. **App URL scheme uses 1500ms visibility-change heuristic.** iOS doesn't return useful errors for URL schemes — visibilitychange / pagehide is the standard pattern. The strategy fires `env.navigate(url)` AFTER subscribing the listener, then races the listener against a 1500ms timeout. The pattern is timing-sensitive; real-device testing will tune. Limitations documented in the strategy file's docblock.
6. **`navigator.share` on iOS is PWA-only for files.** iOS Safari 16.4+ supports `navigator.share({ files })` only inside an installed PWA. F11 detects PWA mode via `is_pwa_installed_on_ios()` (matchMedia('(display-mode: standalone)') OR legacy `navigator.standalone === true`). The PWA POSTs `in_pwa_mode` to `/manual-share/intent`; the server propagates it in the iOS payload. The handler's `filter_chain_by_capabilities` strips `navigator_share_files` when `in_pwa_mode === false` so the runner doesn't waste time fetching files only to discover the API silently drops them.
7. **AbortError is a hard stop, not a fall-through.** When `navigator.share` rejects with `AbortError` (user tapped cancel on the system share sheet), the strategy returns `'aborted'` — NOT `'rejected'`. The runner respects that and stops. Other rejection reasons (NotAllowedError, etc.) return `'rejected'` and the chain continues.
8. **Manual fallback is a real UX, not an error.** `try_manual_fallback` invokes `env.show_manual_modal(props)` which mounts the `ManualShareFallbackModal` component. The modal renders Save Image (when files present), Open App (when platform homepage URL exists), Done buttons. User-explicit dismiss via Done/Save/Open returns `'fired'`; close-without-action returns `'aborted'`. `PLATFORM_HOMEPAGE` map in the strategy hard-codes the platform brand URLs (instagram.com, threads.net, etc.) — not user-specific instances.
9. **Audit-log strategy mapping projects fine-grained iOS chain entries onto coarse storage labels.** The audit log accepts `navigator_share`, `intent_url`, `two_tap_fallback` — three coarse labels. F11's `map_strategy_to_audit_label` projects: `navigator_share_files` → `navigator_share`; `app_url_scheme` + `web_intent` → `intent_url`; `manual` → `two_tap_fallback`. The PWA's telemetry POST (already from F10) updates the entry with the actual fired strategy at completion time.
10. **Async strategy-chain re-entrancy fix.** During F11 test development, the synchronous-test-mock for `set_timeout` triggered a TDZ error: the timer callback fired before `const timer_handle` was initialized. Fix: declare `timer_handle` as `let` initialized to `undefined`; guard `clear_timeout` with `undefined !== timer_handle`; check `resolved` flag before navigating. Captured here so future strategies that race callbacks against timeouts use the working pattern.
11. **PWA composer chip-strip rendering UI remains deferred.** Same rationale as F10 #13: F11 ships the iOS handler + strategies + modal as testable building blocks; the composer-side "tap chip → execute_ios_share" wiring lands when F12 (silo URL capture) provides the after-share UX hook. F12's `prompt_for_silo_url` flow needs the chip-strip to display the prompt; that's the natural integration point.
12. **Live device verification deferred to one-time check.** Per acceptance criterion #15, manually verify on a real iOS device: share Photo post to Instagram via the manual-share chip, confirm strategy chain works end-to-end (PWA Safari + non-PWA Safari paths). Not a CI requirement; documented in the F11 PR as the validation that the timing-sensitive visibility heuristic actually fires for real users.
13. **Naming divergences from prompt.** Prompt: `IosShareHandler.ts`, `IosFallbackModal.tsx`, `strategies/navigatorShareFiles.ts`, `tests/companions/manual_share/Ios_Strategy_ChainsTest.php`. Actual repo conventions: `pwa/src/lib/manual-share/ios.ts`, `pwa/src/components/manual-share-fallback-modal.tsx`, `pwa/src/lib/manual-share/strategies/navigator-share-files.ts` (kebab-case), `tests/unit/IosStrategyChainsTest.php`. Continues F9–F10 pattern.
14. **Tests added: 25 PHP + 46 PWA = 71.** PHP 431 → 456 (`IosStrategyChainsTest` 14, `IntentPayloadBuilderTest` 6 new iOS tests, `ManualShareControllerTest` 3 new iOS tests + 2 updated F10 tests, `PlatformConfigTest` 4 new schema tests + 1 updated). PWA 205 → 251 (`platform.test.ts` 4 new for is_pwa_installed_on_ios, `strategies/*.test.ts` 23 across 4 strategy modules, `strategy-runner.test.ts` 5, `ios.test.ts` 8, `manual-share-fallback-modal.test.tsx` 8). All gates green: PHPCS, PHPStan, §5 audit, PHPUnit, Vitest, TypeScript strict.
15. **No version bump.** F11 ships TypeScript modules + server payload + Platform_Config schema migration. User-observable change ships when the chip-strip wires Android + iOS handlers together.

## Session F12 — Locked Decisions (shipped)

Closed 2026-05-04. Closes the manual-share two-phase loop: when the user returns to Outpost after posting in a destination app, they paste the silo URL into a small inline prompt, Outpost writes a `u-syndication` link to the post's microformats output. Bridgy and other webmention consumers parse the marker to find the silo copy.

1. **Phase-2 capture is non-blocking.** The prompt is a soft nudge — users can ignore it indefinitely; the post stays valid. The detector hides entries past a 30-day retention window so abandoned shares don't accumulate, and within a 30-second grace period after firing so the prompt doesn't appear during the share itself.
2. **URL validation is liberal.** Accept any http/https URL of any domain (custom domains, archived posts, future silos). Reject empty / whitespace, non-http schemes (file://, javascript:, data:), URLs that fail `wp_http_validate_url` (rejects loopback + RFC 1918 + link-local). Length cap 2048 chars.
3. **No silo scraping.** Outpost does NOT fetch the pasted URL to verify it. That would add complexity, fail unpredictably (silos block logged-out fetchers), and not improve the IndieWeb plumbing. Trust the user to paste what they meant.
4. **Soft-warn on platform mismatch.** When the URL's domain doesn't match the platform_id's expected domain (Instagram chip + Twitter URL), the controller returns `status: 'mismatch_warning'` instead of recording. The PWA shows a confirm prompt; user clicks "Save anyway" → resubmits with `confirm_mismatch: true`. Unknown platforms (custom site-registered) skip the mismatch check entirely.
5. **Storage shape: `outpost_syndication_links` post-meta is canonical.** Array of `{platform_id, url, added_at, source}` per entry. Idempotent: writing the same `(platform_id, url)` pair twice updates `added_at` rather than appending. The renderer derives HTML from this meta — meta is the source of truth, not the rendered HTML.
6. **`u-syndication` rendered hidden by default.** The renderer wraps anchors in `<div class="outpost-syndication-links" hidden>` so mf2 parsers see them in the source HTML but visible content stays clean. Themes wanting to show the syndication links override `[hidden]` in CSS or use `outpost_render_syndication_links` filter for full programmatic control. The_excerpt also includes the wrapper so Bridgy's feed-scanning paths work.
7. **Two surfaces: composer panel + WP admin notice.** PWA's `PendingSyndicationsPanel` lists pending entries with inline `SyndicationCaptureForm` (URL input + Save/Cancel + mismatch confirm). WP admin's `Outpost_Pending_Syndication_Notice` appears on post-edit screens listing pending entries, points users at the PWA composer for the inline form (avoids shipping admin-side JS just for the form).
8. **`register_post_meta` exposes `outpost_syndication_links` via WP REST.** Tools that read post objects via `/wp-json/wp/v2/posts/N` get the syndication state programmatically. `auth_callback` requires `edit_posts` cap so the meta only surfaces to authenticated callers (defense-in-depth; the array contains URLs the user posted, not credentials, but consistent with the rest of Outpost's posture).
9. **`Pending_Capture_Detector` accepts a candidate-resolver injection for tests.** Production uses WP_Query with `meta_query` for posts that have the `outpost_manual_share_log` meta key. Unit tests inject a static array of post IDs to bypass WP_Query — simpler than mocking the WP_Query class. Captured here so future detectors that scan post-meta follow the same test seam pattern.
10. **`DAY_IN_SECONDS` / `MINUTE_IN_SECONDS` / `HOUR_IN_SECONDS` defined in tests/bootstrap.php.** F12's detector uses `DAY_IN_SECONDS * $retention_days`. The constants come from WordPress core but the unit-test bootstrap doesn't load core. Added the three time constants to the bootstrap stub; future Phase F+G detectors that use WP time constants benefit.
11. **Webmention triggering on writeback deferred.** F12 prompt mentions sending a webmention to the silo URL on writeback (Pfefferle Webmention plugin pattern). Outpost has no existing webmention sender — there's no F-earlier infrastructure to extend. Documented as a follow-up to F12 / Phase G concern when webmention sender lands.
12. **PWA composer integration deferred (consistent with F10 #13 / F11 #11).** F12 ships the panel + form components + capture-api helper as testable building blocks. Mounting `PendingSyndicationsPanel` in the composer's main view is a separate concern — the natural integration point is when F13 (audit log surfacing) lands and the composer needs to manage all three surfaces (chip strip, pending panel, audit log) together. Tests cover each component in isolation; production integration is a focused follow-up.
13. **Naming divergences from prompt.** Prompt: `Syndication_Writeback.php`, `Pending_Capture_Detector.php`, `Syndication_Capture_Controller.php`, `Pending_Syndication_Notice.php`, `Syndication_Links_Renderer.php`, `tests/companions/manual_share/Syndication_WritebackTest.php`. Actual repo conventions: `class-syndication-writeback.php` (lowercase + dashes); REST controller at `includes/class-syndication-capture-controller.php` (top-level); admin notice + renderer at `includes/class-pending-syndication-notice.php` + `includes/class-syndication-links-renderer.php`; tests in `tests/unit/`. Continues F9–F11 pattern.
14. **Tests added: 51 PHP + 27 PWA = 78.** PHP 456 → 513 (with 6 wp-env-pending integration stubs in `SyndicationCaptureFlowTest`): `SyndicationWritebackTest` 22, `PendingCaptureDetectorTest` 13, `SyndicationCaptureControllerTest` 9, `SyndicationLinksRendererTest` 7. PWA 251 → 278: `capture-api.test.ts` 11, `syndication-capture-form.test.tsx` 7, `pending-syndications-panel.test.tsx` 9. All gates green: PHPCS, PHPStan, §5 audit, PHPUnit, Vitest, TypeScript strict.
15. **No version bump.** F12 ships server-side endpoints + post-meta registration + the_content filter + admin notice + PWA components. The user-observable change ships when the composer integration mounts the panel; F12 is the foundation F13+ wires in.

## Build Order (40 sessions)

Phase A: Foundation (slug, scaffold, companion detector, routes).
Phase B: Auth and Micropub (IndieAuth flow, Micropub client, server-side mf2 preview).
Phase C: Composer modes (Note → Reply → Photo → Listen group → Article → More pull-out).
Phase D: PWA polish (manifest, service worker, offline queue, voice, iOS safe-area).
Phase E: Share-sheet and bookmarklets (Web Share Target, iOS Shortcut, multi-action bookmarklet generator, Bridgy auto-suggest).
Phase F: Companions (Post Kinds, Post Formats, XFN, Yoast, ActivityPub/Bridgy adapters).
Phase G: Security (token hardening, SW review, CSP, file-upload, rate-limit, URL validation, pen test).
Phase H: Settings and onboarding.
Phase I: Distribution (readme.txt, screenshots, banner/icon, submission).
Phase J: Documentation and launch.

Each session is its own conversation, scoped to ~2-3 KB of context. Do not skip ahead.
