# Section 5 audit — allowed test-fixture handle names (B5).
#
# Test fixtures may use these generic names for users / accounts.
# Anything else triggers a B5 lint failure on the assumption it leaks
# a real handle.
#
# Sources: cryptography literature stock names (RFC 4949, NIST examples).

alice
bob
charlie
dave
eve
mallory
trent
walter
peggy
victor

# JSON-LD reserved keywords (https://www.w3.org/TR/json-ld11/#syntax-tokens).
# These appear as @context / @type / @id / @graph on schema.org JSON-LD
# fixtures used by G4b extractor tests. They are syntax tokens, not
# personal handles, and cannot leak personal data.
context
type
id
graph
value
vocab
language
list
set
index
reverse
nest
container
base
included
