# Section 5 audit — allowed canonical fediverse / silo instance hostnames (B3).
#
# Hostnames hardcoded outside this allowlist trigger a B3 lint failure
# on the assumption they're case-study leaks. Rationale:
#
#   - example.* and *.example are RFC 2606 reserved test domains —
#     guaranteed to never resolve to a real instance.
#   - mastodon.social and bsky.social are protocol-spec reference
#     pointers (Mastodon's flagship instance, Bluesky's PDS bootstrap),
#     not endorsements. They appear in canonical IndieWeb docs and
#     Bridgy Fed integration tests where the reference is unavoidable.
#
# Adding a new entry: must come with a doc-citation comment in the
# referencing source file pointing at the spec / canonical reference
# that justifies the hardcoded hostname.

example.com
example.org
example.net
example.social
example.test
mastodon.example
pleroma.example
pixelfed.example
bsky.example
pdsexample.com
mastodon.social
bsky.social
# F17 — Bluesky's canonical post URLs use bsky.app, the official web
# client / protocol entry. Outpost_Source_Bluesky claims this host
# pattern. Not a user-specific instance — Bluesky's flagship surface.
bsky.app
# F17 — Mastodon source-adapter allowlist defaults reference these
# hostnames in its filter callbacks. Each is a flagship / well-known
# instance referenced in the Mastodon community as canonical, not
# a user-specific instance. Site owners override via the
# outpost_mastodon_allowed_hosts filter for their own preferences.
mas.to
hachyderm.io
fosstodon.org
indieweb.social
tech.lgbt
infosec.exchange
home.social
me.dm

# Bridgy / Bridgy Fed bridge endpoints (Ryan Barrett). Used by Outpost
# as Web Share Target / publish proxy URLs. Not user-specific
# instances — bridges between protocols.
brid.gy
bsky.brid.gy

# Canonical platform intent endpoints (F9 Companion_ManualShare). The
# F9 Threads platform config hardcodes the documented web-intent
# endpoint at https://www.threads.net/intent/post — that's a brand
# endpoint, not a user-specific instance. Threads is the only F9
# platform whose hostname matches B3's fediverse-shaped regex
# (subdomain.threads.net); the other 9 platforms (twitter.com,
# pinterest.com, reddit.com, etc.) bypass the regex entirely so don't
# need allowlisting.
www.threads.net
